---
phase: 01-foundation-deployable-skeleton
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - public/favicon.svg
  - public/robots.txt
  - src/layouts/BaseLayout.astro
  - src/components/layout/Nav.astro
  - src/components/layout/Footer.astro
  - src/components/layout/WhatsAppFloat.astro
  - src/data/site.ts
  - src/data/nav.ts
  - src/data/services.ts
  - src/data/contact.ts
  - src/pages/index.astro
  - src/pages/acompanamiento.astro
  - src/pages/retiros.astro
  - src/pages/contacto.astro
  - src/pages/404.astro
autonomous: true
requirements: [FOUND-03, FOUND-05, FOUND-06, FOUND-07, FOUND-08, FOUND-09]
tags: [layout, pages, components, noindex, robots, 404, favicon, header-blur, wordmark]

must_haves:
  truths:
    - "Las 4 rutas (`/`, `/acompanamiento`, `/retiros`, `/contacto`) renderizan vía BaseLayout y producen un HTML válido tras `npm run build`"
    - "Cada página HTML contiene `<meta name=\"robots\" content=\"noindex,nofollow\">` (FOUND-08)"
    - "`public/robots.txt` contiene `User-agent: *` y `Disallow: /` (FOUND-08)"
    - "Header transparente con wordmark Fraunces 300 lowercase tracking-wide + nav links + hamburger móvil (D-06, D-07, D-09)"
    - "Footer presente con brand line + status noindex"
    - "WhatsAppFloat sticky con placeholder `wa.me/0000000000` (Phase 1 stub per CONTEXT.md Deferred)"
    - "Custom `404.astro` renderiza con tono cósmico-amable y link de regreso al inicio"
    - "Favicon SVG circular gradiente radial cosmic referenciado desde BaseLayout `<head>`"
    - "Todos los internal links usan `import.meta.env.BASE_URL` (Pitfall #13 mitigation)"
    - "Header aplica `backdrop-filter: blur(12px)` cuando `window.scrollY > 50` (D-09)"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "HTML shell — head, fonts, OG defaults, noindex meta, Nav + Footer + WhatsAppFloat + slot"
      min_lines: 50
    - path: "src/components/layout/Nav.astro"
      provides: "Sticky transparent header → blur-on-scroll; wordmark + nav links + mobile hamburger"
      min_lines: 30
    - path: "src/components/layout/Footer.astro"
      provides: "Footer with brand line + year + noindex status"
      min_lines: 10
    - path: "src/components/layout/WhatsAppFloat.astro"
      provides: "Floating sticky CTA with wa.me placeholder"
      min_lines: 10
    - path: "src/data/site.ts"
      provides: "Site name, base URL, OG defaults, indexed:false flag"
      contains: "planetapsilo"
    - path: "src/data/nav.ts"
      provides: "Nav items array (Inicio, Acompañamiento, Retiros, Contacto)"
      contains: "Acompañamiento"
    - path: "src/data/services.ts"
      provides: "Service teaser placeholders (Phase 2 replaces with real copy)"
    - path: "src/data/contact.ts"
      provides: "Stubs: WhatsApp number, Calendly URL placeholder"
      contains: "0000000000"
    - path: "public/favicon.svg"
      provides: "Circular SVG with radial cosmic gradient (D-08)"
      contains: "radialGradient"
    - path: "public/robots.txt"
      provides: "Disallow: / until Phase 3"
      contains: "Disallow: /"
    - path: "src/pages/index.astro"
      provides: "Home stub composing BaseLayout + hero placeholder"
    - path: "src/pages/acompanamiento.astro"
      provides: "Service stub"
    - path: "src/pages/retiros.astro"
      provides: "Retreats stub"
    - path: "src/pages/contacto.astro"
      provides: "Contact stub with 3-channel placeholders"
    - path: "src/pages/404.astro"
      provides: "Cosmic-toned 404 with link home"
  key_links:
    - from: "Each page in src/pages/"
      to: "BaseLayout"
      via: "import BaseLayout from '../layouts/BaseLayout.astro'"
      pattern: "import BaseLayout"
    - from: "BaseLayout"
      to: "src/styles/global.css"
      via: "import '../styles/global.css'"
      pattern: "global\\.css"
    - from: "Nav internal links"
      to: "computed BASE_URL"
      via: "import.meta.env.BASE_URL"
      pattern: "BASE_URL"
    - from: "BaseLayout <head>"
      to: "noindex meta tag"
      via: "<meta name=\"robots\" content=\"noindex,nofollow\">"
      pattern: "noindex"
---

<objective>
Componer el esqueleto visible del sitio sobre la base ya instalada (scaffold de Plan 01 + tokens de Plan 02): BaseLayout con `<head>` completo, paquete de componentes de chrome (Nav transparente con blur-on-scroll, Footer, WhatsAppFloat sticky), las 4 page stubs (`/`, `/acompanamiento`, `/retiros`, `/contacto`), un 404.astro custom, los `src/data/*.ts` con tipado básico, favicon SVG circular cosmic-gradient, y los gates legales de Phase 1: `<meta name="robots" content="noindex,nofollow">` en cada página + `public/robots.txt` con `Disallow: /`.

Purpose: Este es el plan más grande del phase porque entrega lo que se ve en `https://elsolarcg.github.io/planetapsilo/` cuando Plan 05 deploye. Implementa D-06 (wordmark "planetapsilo" lowercase Fraunces 300), D-07 (tratamiento del wordmark), D-08 (favicon SVG radial), D-09 (header blur on scroll). Honra el constraint de noindex desde commit 1 (Pitfall #22) y el constraint de stubs (no datos reales, no copy real — eso es Phase 2).

Output: Un build (`npm run build`) que produce `dist/` con `index.html`, `acompanamiento/index.html`, `retiros/index.html`, `contacto/index.html`, `404.html` y `favicon.svg` + `robots.txt`. Cada HTML contiene noindex, referencia assets bajo `/planetapsilo/`, y muestra el chrome completo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
@CLAUDE.md
@astro.config.mjs
@src/styles/global.css
@src/styles/tokens.css

<interfaces>
<!-- This plan creates the contract that Phase 2 will consume. -->

Data shape contracts (TypeScript) that Phase 2 will fill with real copy:

```ts
// src/data/site.ts
export interface SiteConfig {
  name: string;          // "planetapsilo"
  baseUrl: string;       // import.meta.env.SITE
  ogDefault: {
    title: string;
    description: string;
    image: string;       // path relative to BASE_URL
  };
  indexable: boolean;    // false in Phase 1 (drives noindex meta)
  year: number;          // for footer
}
```

```ts
// src/data/nav.ts
export interface NavItem {
  label: string;         // visible text ("Inicio", "Acompañamiento", ...)
  href: string;          // path WITHOUT base prefix (e.g. "/" or "/retiros")
}
export const nav: NavItem[];
```

```ts
// src/data/services.ts
export interface ServicePlaceholder {
  slug: 'acompanamiento' | 'retiros';
  label: string;
  teaser: string;        // 1-2 line placeholder — Phase 2 replaces
}
export const services: ServicePlaceholder[];
```

```ts
// src/data/contact.ts
export interface ContactConfig {
  whatsappNumber: string;    // stub: "0000000000"
  whatsappPrefill: string;   // "Hola%20planetapsilo"
  calendlyUrl: string;       // stub: "#"
}
export const contact: ContactConfig;
```

BaseLayout props contract (Phase 2 will pass per-page overrides):
```ts
interface Props {
  title?: string;            // defaults to site.name
  description?: string;      // defaults to site.ogDefault.description
  ogImage?: string;          // defaults to site.ogDefault.image
}
```

Internal link helper pattern (used in Nav.astro, every page):
```astro
---
const base = import.meta.env.BASE_URL;  // "/planetapsilo/"
const internal = (p: string) => `${base}${p.replace(/^\//, '')}`;
---
<a href={internal('retiros')}>Retiros</a>  <!-- resolves to /planetapsilo/retiros -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `src/data/*.ts` + `public/favicon.svg` + `public/robots.txt`</name>
  <files>src/data/site.ts, src/data/nav.ts, src/data/services.ts, src/data/contact.ts, public/favicon.svg, public/robots.txt</files>
  <read_first>
    - .planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md (Specifics: favicon SVG + wordmark target; Pre-decided: stubs for WhatsApp/Calendly; D-06/D-07/D-08)
    - .planning/research/ARCHITECTURE.md (Phase 1 minimum viable tree: src/data/ structure)
    - .planning/research/PITFALLS.md (Pitfall #22: robots.txt Disallow: / in MVP)
  </read_first>
  <action>
1. Create `src/data/site.ts`:
   ```ts
   // src/data/site.ts
   // Single source of truth for site-wide config consumed by BaseLayout + Footer.
   // Phase 2 will update copy; Phase 3 will flip `indexable` to true after abogado sign-off.

   export interface SiteConfig {
     name: string;
     baseUrl: string;
     ogDefault: {
       title: string;
       description: string;
       image: string; // path relative to BASE_URL (no leading slash)
     };
     indexable: boolean;
     year: number;
   }

   export const site: SiteConfig = {
     name: 'planetapsilo',
     baseUrl: 'https://elsolarcg.github.io/planetapsilo',
     ogDefault: {
       title: 'planetapsilo',
       description: 'Espacio de exploración y acompañamiento. (Sitio en construcción — Phase 1 skeleton.)',
       image: 'og-default.svg',
     },
     // Phase 1 ships noindex. Flipping this to true is a Phase 3 LEGAL-12 task gated by abogado.
     indexable: false,
     year: new Date().getFullYear(),
   };
   ```

2. Create `src/data/nav.ts`:
   ```ts
   // src/data/nav.ts
   export interface NavItem {
     label: string;
     href: string; // path WITHOUT base prefix — internal() helper applies BASE_URL
   }

   export const nav: NavItem[] = [
     { label: 'Inicio',         href: '/' },
     { label: 'Acompañamiento', href: '/acompanamiento' },
     { label: 'Retiros',        href: '/retiros' },
     { label: 'Contacto',       href: '/contacto' },
   ];
   ```

3. Create `src/data/services.ts`:
   ```ts
   // src/data/services.ts
   // Phase 1 stubs. Phase 2 (CONT-02, CONT-03) replaces teaser with real legal-safe copy
   // from docs/copy-glossary.md.

   export interface ServicePlaceholder {
     slug: 'acompanamiento' | 'retiros';
     label: string;
     teaser: string;
   }

   export const services: ServicePlaceholder[] = [
     {
       slug: 'acompanamiento',
       label: 'Acompañamiento',
       teaser: 'Espacio de exploración para C-Level, nómadas digitales y founders. (Copy real en Phase 2.)',
     },
     {
       slug: 'retiros',
       label: 'Retiros',
       teaser: 'Tres fases: Preparación, Inmersión, Integración. (Aplicación obligatoria — formato en Phase 2.)',
     },
   ];
   ```

4. Create `src/data/contact.ts`:
   ```ts
   // src/data/contact.ts
   // Phase 1 stubs (CONTEXT.md Deferred: real channels wired in Phase 2/3).

   export interface ContactConfig {
     whatsappNumber: string;
     whatsappPrefill: string;
     calendlyUrl: string;
   }

   export const contact: ContactConfig = {
     whatsappNumber: '0000000000', // stub — Phase 2 wires real number
     whatsappPrefill: 'Hola%20planetapsilo',
     calendlyUrl: '#', // stub — Phase 3 wires real Calendly
   };
   ```

5. Create `public/favicon.svg` from D-08 spec (radial gradient orange-mystic → violet-deep → blue-cosmic):
   ```svg
   <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
     <defs>
       <radialGradient id="cosmic" cx="50%" cy="50%" r="50%">
         <stop offset="0%" stop-color="#ff7a3d"/>
         <stop offset="55%" stop-color="#5b2a86"/>
         <stop offset="100%" stop-color="#0b1d4f"/>
       </radialGradient>
     </defs>
     <circle cx="16" cy="16" r="14" fill="url(#cosmic)"/>
   </svg>
   ```

6. Create `public/robots.txt` (Pitfall #22 — noindex from commit 1):
   ```
   # planetapsilo — Phase 1: site exists but is not indexable until Phase 3 legal pass (LEGAL-12).
   User-agent: *
   Disallow: /
   ```

7. Create `public/og-default.svg` (simple gradient card 1200x630 — placeholder, Phase 2 replaces with branded version):
   ```svg
   <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630" width="1200" height="630">
     <defs>
       <linearGradient id="og" x1="0" y1="0" x2="1" y2="1">
         <stop offset="0%" stop-color="#5b2a86"/>
         <stop offset="55%" stop-color="#ff7a3d"/>
         <stop offset="100%" stop-color="#0b1d4f"/>
       </linearGradient>
     </defs>
     <rect width="1200" height="630" fill="#050a1a"/>
     <rect width="1200" height="630" fill="url(#og)" opacity="0.7"/>
     <text x="600" y="340" text-anchor="middle" font-family="serif" font-size="84" font-weight="300" fill="#f4ede3" letter-spacing="8">planetapsilo</text>
   </svg>
   ```
  </action>
  <verify>
    <automated>cd /home/plazasia/workspace/07_DIGITAL_GROWTH/clientes/planetapsilo && test -f src/data/site.ts && test -f src/data/nav.ts && test -f src/data/services.ts && test -f src/data/contact.ts && test -f public/favicon.svg && test -f public/robots.txt && test -f public/og-default.svg && grep -q "indexable: false" src/data/site.ts && grep -q "Acompañamiento" src/data/nav.ts && grep -q "0000000000" src/data/contact.ts && grep -q "Disallow: /" public/robots.txt && grep -q "radialGradient" public/favicon.svg</automated>
  </verify>
  <acceptance_criteria>
    - All four `src/data/*.ts` files exist with exported interfaces + values
    - `site.indexable === false` (drives noindex meta in BaseLayout — Task 2)
    - `nav` array has exactly 4 entries: Inicio, Acompañamiento, Retiros, Contacto
    - `contact.whatsappNumber === '0000000000'` (Phase 1 stub per CONTEXT.md Deferred)
    - `contact.calendlyUrl === '#'` (Phase 1 stub)
    - `public/favicon.svg` exists and contains a `<radialGradient>` (D-08)
    - `public/robots.txt` exists with `User-agent: *` and `Disallow: /`
    - `public/og-default.svg` exists (OG fallback)
  </acceptance_criteria>
  <done>
Data layer and `public/` assets are in place. BaseLayout (Task 2) can now import from `src/data/*` and reference `/favicon.svg` + `/og-default.svg`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build BaseLayout + Nav + Footer + WhatsAppFloat chrome components</name>
  <files>src/layouts/BaseLayout.astro, src/components/layout/Nav.astro, src/components/layout/Footer.astro, src/components/layout/WhatsAppFloat.astro</files>
  <read_first>
    - .planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md (D-06/D-07/D-09 wordmark + header treatment + "Inspiración: Sabiduría + Stripe blur"; Deferred section: WhatsApp stub `wa.me/0000000000`)
    - .planning/research/ARCHITECTURE.md (Pattern 1 Layout → Page composition; Pattern 4 three-channel contact; Phase 1 component tree at src/components/layout/)
    - .planning/research/PITFALLS.md (Pitfall #15: target="_blank" + rel + microcopy on WhatsApp; Pitfall #13: BASE_URL on every internal link)
    - src/styles/global.css and src/styles/tokens.css (already authored by Plan 02 — these are the consumable tokens)
  </read_first>
  <action>
1. Create `src/layouts/BaseLayout.astro`:
   ```astro
   ---
   // src/layouts/BaseLayout.astro
   // HTML shell consumed by every page (FOUND-03).
   // - Imports global.css (which imports tokens.css + Tailwind + Google Fonts) — Plan 02
   // - Renders Nav (sticky, blur-on-scroll), Footer, WhatsAppFloat (sticky CTA)
   // - Emits noindex meta + OG defaults from src/data/site.ts (FOUND-08)
   // - Composes the page via <slot />
   //
   // Phase 1: site.indexable === false → meta robots noindex,nofollow.
   // Phase 3 (LEGAL-12): flip site.indexable to true; this layout will then omit the meta tag.

   import '../styles/global.css';
   import { site } from '../data/site';
   import Nav from '../components/layout/Nav.astro';
   import Footer from '../components/layout/Footer.astro';
   import WhatsAppFloat from '../components/layout/WhatsAppFloat.astro';

   interface Props {
     title?: string;
     description?: string;
     ogImage?: string;
   }

   const { title, description, ogImage } = Astro.props as Props;
   const pageTitle = title ? `${title} — ${site.name}` : site.name;
   const pageDescription = description ?? site.ogDefault.description;
   const baseUrl = import.meta.env.BASE_URL; // resolves to "/planetapsilo/" at build
   const ogImgPath = `${baseUrl}${(ogImage ?? site.ogDefault.image).replace(/^\//, '')}`;
   const canonical = `${site.baseUrl}${Astro.url.pathname.replace(baseUrl.replace(/\/$/, ''), '')}`;
   ---
   <!doctype html>
   <html lang="es">
     <head>
       <meta charset="utf-8" />
       <meta name="viewport" content="width=device-width, initial-scale=1" />
       <title>{pageTitle}</title>
       <meta name="description" content={pageDescription} />

       {/* Phase 1 indexation gate: while site.indexable === false, do NOT let crawlers index */}
       {!site.indexable && <meta name="robots" content="noindex,nofollow" />}
       {!site.indexable && <meta name="googlebot" content="noindex,nofollow" />}

       <link rel="icon" href={`${baseUrl}favicon.svg`} type="image/svg+xml" />
       <link rel="canonical" href={canonical} />

       {/* OG defaults — Phase 2 may override per page */}
       <meta property="og:title" content={pageTitle} />
       <meta property="og:description" content={pageDescription} />
       <meta property="og:image" content={`${site.baseUrl}/${(ogImage ?? site.ogDefault.image).replace(/^\//, '')}`} />
       <meta property="og:type" content="website" />
       <meta property="og:locale" content="es_CO" />

       {/* preconnect to Google Fonts CDN (font load is in global.css) */}
       <link rel="preconnect" href="https://fonts.googleapis.com" />
       <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
     </head>
     <body>
       <Nav />
       <main>
         <slot />
       </main>
       <Footer />
       <WhatsAppFloat />
     </body>
   </html>
   ```

2. Create `src/components/layout/Nav.astro` (D-06, D-07, D-09):
   ```astro
   ---
   // src/components/layout/Nav.astro
   // Sticky transparent header → backdrop-filter: blur(12px) when window.scrollY > 50 (D-09).
   // Wordmark "planetapsilo" lowercase Fraunces 300 tracking-wide (D-06, D-07).
   // Mobile (< 768px): wordmark + hamburger; nav collapses to fullscreen drawer (D-09).

   import { nav } from '../../data/nav';

   const baseUrl = import.meta.env.BASE_URL; // "/planetapsilo/"
   const internal = (p: string) => `${baseUrl}${p.replace(/^\//, '')}`;
   ---
   <header id="site-header" class="site-header">
     <div class="container nav-row">
       <a href={internal('/')} class="wordmark" aria-label="planetapsilo — inicio">planetapsilo</a>

       <button
         id="nav-toggle"
         class="nav-toggle"
         aria-label="Abrir menú"
         aria-expanded="false"
         aria-controls="primary-nav"
       >
         <span aria-hidden="true"></span>
         <span aria-hidden="true"></span>
         <span aria-hidden="true"></span>
       </button>

       <nav id="primary-nav" class="primary-nav" aria-label="Navegación principal">
         <ul>
           {nav.map((item) => (
             <li><a href={internal(item.href)}>{item.label}</a></li>
           ))}
         </ul>
       </nav>
     </div>
   </header>

   <style>
     .site-header {
       position: sticky;
       top: 0;
       z-index: 50;
       background: transparent;
       transition: background-color 200ms ease, backdrop-filter 200ms ease, border-color 200ms ease;
       border-bottom: 1px solid transparent;
     }
     .site-header.scrolled {
       background-color: rgba(5, 10, 26, 0.6);
       backdrop-filter: blur(12px);
       -webkit-backdrop-filter: blur(12px);
       border-bottom: 1px solid var(--color-chrome-border);
     }
     .nav-row {
       display: flex;
       align-items: center;
       justify-content: space-between;
       padding-block: 1rem;
     }
     .wordmark {
       font-family: var(--font-display);
       font-weight: 300;
       font-size: 1.5rem;
       letter-spacing: 0.08em;
       text-transform: lowercase;
       color: var(--color-text-primary);
     }
     .primary-nav ul {
       display: flex;
       gap: 2rem;
       list-style: none;
       margin: 0;
       padding: 0;
     }
     .primary-nav a {
       font-family: var(--font-body);
       font-size: 0.95rem;
       font-weight: 400;
       color: var(--color-text-primary);
     }
     .primary-nav a:hover,
     .primary-nav a:focus-visible {
       color: var(--color-orange-mystic);
     }
     .nav-toggle {
       display: none;
       background: none;
       border: none;
       padding: 0.5rem;
       cursor: pointer;
       flex-direction: column;
       gap: 5px;
     }
     .nav-toggle span {
       display: block;
       width: 24px;
       height: 2px;
       background: var(--color-text-primary);
       transition: transform 200ms ease, opacity 200ms ease;
     }

     @media (max-width: 767px) {
       .nav-toggle { display: flex; }
       .primary-nav {
         position: fixed;
         inset: 0;
         background: var(--color-bg-base);
         display: none;
         padding: 6rem 2rem 2rem;
       }
       .primary-nav.open { display: block; }
       .primary-nav ul {
         flex-direction: column;
         gap: 1.5rem;
         font-size: 1.25rem;
       }
     }
   </style>

   <script>
     // D-09: blur-on-scroll. Vanilla JS so we ship 0 KB of framework runtime.
     const header = document.getElementById('site-header');
     const toggle = document.getElementById('nav-toggle');
     const navEl = document.getElementById('primary-nav');

     const onScroll = () => {
       if (!header) return;
       if (window.scrollY > 50) header.classList.add('scrolled');
       else header.classList.remove('scrolled');
     };
     window.addEventListener('scroll', onScroll, { passive: true });
     onScroll();

     if (toggle && navEl) {
       toggle.addEventListener('click', () => {
         const isOpen = navEl.classList.toggle('open');
         toggle.setAttribute('aria-expanded', String(isOpen));
         toggle.setAttribute('aria-label', isOpen ? 'Cerrar menú' : 'Abrir menú');
       });
     }
   </script>
   ```

3. Create `src/components/layout/Footer.astro`:
   ```astro
   ---
   // src/components/layout/Footer.astro
   // Phase 1 minimal footer. Phase 2 (CONT-10, CONT-11) wires social links + privacy link + disclaimer.

   import { site } from '../../data/site';
   ---
   <footer class="site-footer">
     <div class="container footer-row">
       <p class="brand-line">
         <span class="wordmark">{site.name}</span>
         <span class="separator">·</span>
         <span class="muted">{site.year}</span>
         {!site.indexable && (
           <>
             <span class="separator">·</span>
             <span class="status muted">Sitio en construcción — sigue noindex</span>
           </>
         )}
       </p>
     </div>
   </footer>

   <style>
     .site-footer {
       margin-top: var(--space-section);
       padding-block: 2rem;
       border-top: 1px solid var(--color-chrome-border);
     }
     .footer-row {
       display: flex;
       align-items: center;
       justify-content: center;
       text-align: center;
     }
     .brand-line {
       margin: 0;
       font-family: var(--font-body);
       font-size: 0.875rem;
       color: var(--color-text-muted);
       display: flex;
       gap: 0.5rem;
       align-items: center;
       flex-wrap: wrap;
       justify-content: center;
     }
     .wordmark {
       font-family: var(--font-display);
       font-weight: 300;
       letter-spacing: 0.08em;
       text-transform: lowercase;
       color: var(--color-text-primary);
     }
     .muted { color: var(--color-text-muted); }
     .separator { opacity: 0.5; }
   </style>
   ```

4. Create `src/components/layout/WhatsAppFloat.astro`:
   ```astro
   ---
   // src/components/layout/WhatsAppFloat.astro
   // Phase 1: stub button. Phase 2 (CONT-05) wires real number + microcopy.
   // Pitfall #15: target="_blank" + rel="noopener noreferrer" mandatory.

   import { contact } from '../../data/contact';

   const href = `https://wa.me/${contact.whatsappNumber}?text=${contact.whatsappPrefill}`;
   ---
   <a
     class="wa-float"
     href={href}
     target="_blank"
     rel="noopener noreferrer"
     aria-label="Abrir WhatsApp (stub — Phase 2 conecta número real)"
   >
     <svg viewBox="0 0 24 24" width="28" height="28" fill="currentColor" aria-hidden="true">
       <path d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 2.1.55 4.14 1.6 5.95L2 22l4.27-1.4a9.92 9.92 0 0 0 5.77 1.83h.01c5.46 0 9.91-4.45 9.91-9.91 0-2.65-1.03-5.14-2.9-7.02A9.84 9.84 0 0 0 12.04 2zm5.43 14.05c-.23.65-1.34 1.25-1.86 1.31-.5.06-.95.08-1.55-.1-.36-.11-.83-.27-1.43-.53-2.5-1.08-4.14-3.6-4.27-3.77-.12-.16-1.02-1.36-1.02-2.6 0-1.23.65-1.84.88-2.1.23-.25.5-.31.66-.31h.47c.16 0 .37-.06.58.45.21.5.71 1.74.78 1.86.06.13.1.27.02.43-.08.16-.12.25-.24.4-.12.13-.25.31-.36.42-.12.13-.24.27-.1.5.13.25.6 1.01 1.3 1.64.88.79 1.62 1.04 1.86 1.16.23.12.36.1.5-.06.13-.16.58-.68.74-.92.16-.23.31-.2.52-.12.21.08 1.36.64 1.6.76.23.12.39.18.45.27.06.1.06.56-.17 1.21z"/>
     </svg>
   </a>

   <style>
     .wa-float {
       position: fixed;
       right: 1.25rem;
       bottom: 1.25rem;
       z-index: 40;
       display: inline-flex;
       align-items: center;
       justify-content: center;
       width: 56px;
       height: 56px;
       border-radius: 999px;
       background: var(--color-orange-mystic);
       color: var(--color-bg-base);
       box-shadow: 0 8px 24px rgba(255, 122, 61, 0.35);
       transition: transform 200ms ease;
     }
     .wa-float:hover { transform: scale(1.05); }
     @media (prefers-reduced-motion: reduce) {
       .wa-float { transition: none; }
       .wa-float:hover { transform: none; }
     }
   </style>
   ```
  </action>
  <verify>
    <automated>cd /home/plazasia/workspace/07_DIGITAL_GROWTH/clientes/planetapsilo && test -f src/layouts/BaseLayout.astro && test -f src/components/layout/Nav.astro && test -f src/components/layout/Footer.astro && test -f src/components/layout/WhatsAppFloat.astro && grep -q "noindex,nofollow" src/layouts/BaseLayout.astro && grep -q "import.meta.env.BASE_URL" src/layouts/BaseLayout.astro && grep -q "import.meta.env.BASE_URL" src/components/layout/Nav.astro && grep -q "backdrop-filter: blur" src/components/layout/Nav.astro && grep -q "window.scrollY > 50" src/components/layout/Nav.astro && grep -q "planetapsilo" src/components/layout/Nav.astro && grep -q "wa.me" src/components/layout/WhatsAppFloat.astro && grep -q "target=\"_blank\"" src/components/layout/WhatsAppFloat.astro && grep -q "rel=\"noopener noreferrer\"" src/components/layout/WhatsAppFloat.astro && grep -q "site.indexable" src/components/layout/Footer.astro</automated>
  </verify>
  <acceptance_criteria>
    - `src/layouts/BaseLayout.astro` exists and contains literal `noindex,nofollow` (FOUND-08 gate)
    - BaseLayout uses `import.meta.env.BASE_URL` for favicon and OG paths (Pitfall #13 mitigation)
    - `src/components/layout/Nav.astro` contains the literal wordmark "planetapsilo" + `backdrop-filter: blur` + `window.scrollY > 50` (D-09)
    - Nav.astro uses `import.meta.env.BASE_URL` for every internal link
    - `src/components/layout/Footer.astro` exists and references `site.indexable` to render the noindex status string (CONT-10 stub)
    - `src/components/layout/WhatsAppFloat.astro` contains literal `wa.me`, `target="_blank"`, `rel="noopener noreferrer"` (Pitfall #15 mitigation)
    - WhatsAppFloat respects `prefers-reduced-motion: reduce` (no hover scale)
  </acceptance_criteria>
  <done>
Chrome components are composed. BaseLayout is ready to be consumed by the 4 page stubs (Task 3) + the 404 page.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 4 page stubs + custom 404.astro</name>
  <files>src/pages/index.astro, src/pages/acompanamiento.astro, src/pages/retiros.astro, src/pages/contacto.astro, src/pages/404.astro</files>
  <read_first>
    - .planning/phases/01-foundation-deployable-skeleton/01-CONTEXT.md (Specifics: hero gradient example using .hero-bg; D-04 cosmic-rotate; 404 tone "Te perdiste en el universo de planetapsilo. Volvé al inicio.")
    - .planning/research/ARCHITECTURE.md (Phase 1 minimum viable tree: 5 .astro files in src/pages/)
    - .planning/research/PITFALLS.md (Pitfall #8: hero impact + contraste WCAG AA — even stubs should not fail a11y)
  </read_first>
  <action>
1. Overwrite `src/pages/index.astro` (this replaces the temporary stub from Plan 01 Task 1):
   ```astro
   ---
   // src/pages/index.astro — Home
   // Phase 1 stub: hero with .hero-bg animated cosmic gradient + wordmark + placeholder máxima.
   // Phase 2 (CONT-01) replaces with real value-prop + dual CTA.

   import BaseLayout from '../layouts/BaseLayout.astro';
   ---
   <BaseLayout
     title="Inicio"
     description="planetapsilo — espacio de exploración y acompañamiento. (Sitio en construcción.)"
   >
     <section class="hero">
       <div class="hero-bg" aria-hidden="true"></div>
       <div class="noise-overlay" aria-hidden="true"></div>
       <div class="container hero-content">
         <p class="kicker">planetapsilo</p>
         <h1 class="hero-headline">
           Un espacio de exploración<br />en construcción.
         </h1>
         <p class="hero-sub">
           Esqueleto Phase 1 — copy real, retiros, acompañamiento y contacto llegan en Phase 2.
         </p>
       </div>
     </section>
   </BaseLayout>

   <style>
     .hero {
       position: relative;
       isolation: isolate;
       min-height: min(100vh, 720px);
       display: flex;
       align-items: center;
       overflow: hidden;
     }
     .hero-bg {
       position: absolute;
       inset: -10%;
       z-index: -2;
       opacity: 0.85;
     }
     .hero-content {
       position: relative;
       z-index: 1;
       text-align: center;
       padding-block: var(--space-section);
     }
     .kicker {
       font-family: var(--font-display);
       font-weight: 300;
       font-size: 1.125rem;
       letter-spacing: 0.16em;
       text-transform: lowercase;
       color: var(--color-text-primary);
       opacity: 0.8;
       margin-bottom: 1.5rem;
     }
     .hero-headline {
       font-family: var(--font-display);
       font-weight: 300;
       font-size: clamp(2.5rem, 6vw, 4.5rem);
       line-height: 1.1;
       letter-spacing: -0.01em;
       color: var(--color-text-primary);
       margin-bottom: 1.5rem;
     }
     .hero-sub {
       font-family: var(--font-body);
       font-size: 1rem;
       max-width: 36rem;
       margin-inline: auto;
       color: var(--color-text-muted);
     }
   </style>
   ```

2. Create `src/pages/acompanamiento.astro`:
   ```astro
   ---
   // src/pages/acompanamiento.astro — Acompañamiento stub (Phase 2 CONT-02 fills with real copy)
   import BaseLayout from '../layouts/BaseLayout.astro';
   ---
   <BaseLayout title="Acompañamiento" description="Espacio de acompañamiento — Phase 1 stub.">
     <section class="page-section">
       <div class="container">
         <p class="kicker">planetapsilo</p>
         <h1>Acompañamiento</h1>
         <p class="lede">
           Para C-Level, founders y nómadas digitales que están reordenando.
           Copy real con registro híbrido (business + filosófico) llega en Phase 2 — sin lenguaje clínico, sin atribución de terapia.
         </p>
       </div>
     </section>
   </BaseLayout>

   <style>
     .page-section { padding-block: var(--space-section); }
     .kicker {
       font-family: var(--font-display);
       font-weight: 300;
       font-size: 1rem;
       letter-spacing: 0.16em;
       text-transform: lowercase;
       opacity: 0.75;
       margin-bottom: 1rem;
     }
     h1 {
       font-family: var(--font-display);
       font-weight: 300;
       font-size: clamp(2rem, 5vw, 3.5rem);
       margin-bottom: 1.5rem;
     }
     .lede {
       font-family: var(--font-body);
       max-width: 42rem;
       color: var(--color-text-muted);
     }
   </style>
   ```

3. Create `src/pages/retiros.astro`:
   ```astro
   ---
   // src/pages/retiros.astro — Retiros stub (Phase 2 CONT-03 fills three-fase structure + application gate)
   import BaseLayout from '../layouts/BaseLayout.astro';
   ---
   <BaseLayout title="Retiros" description="Retiros — Phase 1 stub. Aplicación obligatoria; sin sustancia, sin precio.">
     <section class="page-section">
       <div class="container">
         <p class="kicker">planetapsilo</p>
         <h1>Retiros</h1>
         <p class="lede">
           Tres fases: <em>Preparación · Inmersión · Integración</em>.
           Phase 2 describe el formato (días, ubicación, comidas, círculos de palabra, naturaleza, meditación) — sin nombrar sustancia, sin precio, con aplicación obligatoria.
         </p>
       </div>
     </section>
   </BaseLayout>

   <style>
     .page-section { padding-block: var(--space-section); }
     .kicker {
       font-family: var(--font-display);
       font-weight: 300;
       letter-spacing: 0.16em;
       text-transform: lowercase;
       opacity: 0.75;
       margin-bottom: 1rem;
     }
     h1 {
       font-family: var(--font-display);
       font-weight: 300;
       font-size: clamp(2rem, 5vw, 3.5rem);
       margin-bottom: 1.5rem;
     }
     .lede {
       max-width: 42rem;
       color: var(--color-text-muted);
     }
     em { color: var(--color-orange-mystic); font-style: normal; }
   </style>
   ```

4. Create `src/pages/contacto.astro`:
   ```astro
   ---
   // src/pages/contacto.astro — Contacto stub (Phase 2 CONT-04 wires real 3-channel CTAs)
   import BaseLayout from '../layouts/BaseLayout.astro';
   import { contact } from '../data/contact';

   const waHref = `https://wa.me/${contact.whatsappNumber}?text=${contact.whatsappPrefill}`;
   ---
   <BaseLayout title="Contacto" description="Tres canales — Calendly, WhatsApp, formulario. Phase 1 muestra stubs.">
     <section class="page-section">
       <div class="container">
         <p class="kicker">planetapsilo</p>
         <h1>Contacto</h1>
         <p class="lede">
           Tres canales abiertos. Phase 2 cablea cada uno con datos reales y un formulario Web3Forms con checkbox de autorización Habeas Data.
         </p>
         <div class="cta-row">
           <a class="cta cta-primary" href={contact.calendlyUrl}>Agenda (stub)</a>
           <a class="cta cta-secondary" href={waHref} target="_blank" rel="noopener noreferrer">WhatsApp (stub)</a>
           <a class="cta cta-tertiary" href="#form">Formulario (Phase 2)</a>
         </div>
       </div>
     </section>
   </BaseLayout>

   <style>
     .page-section { padding-block: var(--space-section); }
     .kicker {
       font-family: var(--font-display);
       font-weight: 300;
       letter-spacing: 0.16em;
       text-transform: lowercase;
       opacity: 0.75;
       margin-bottom: 1rem;
     }
     h1 {
       font-family: var(--font-display);
       font-weight: 300;
       font-size: clamp(2rem, 5vw, 3.5rem);
       margin-bottom: 1.5rem;
     }
     .lede { max-width: 42rem; color: var(--color-text-muted); margin-bottom: 2rem; }
     .cta-row { display: flex; gap: 1rem; flex-wrap: wrap; }
     .cta {
       display: inline-block;
       padding: 0.875rem 1.5rem;
       border-radius: 0.5rem;
       font-family: var(--font-body);
       font-weight: 500;
       font-size: 0.95rem;
     }
     .cta-primary { background: var(--color-orange-mystic); color: var(--color-bg-base); }
     .cta-secondary {
       background: transparent;
       color: var(--color-text-primary);
       border: 1px solid var(--color-chrome-border-strong);
     }
     .cta-tertiary {
       background: transparent;
       color: var(--color-text-muted);
       border: 1px solid var(--color-chrome-border);
     }
   </style>
   ```

5. Create `src/pages/404.astro` (FOUND-09):
   ```astro
   ---
   // src/pages/404.astro — Custom 404 (FOUND-09).
   // Tone (CONTEXT.md Specifics): cósmico-amable. "Te perdiste en el universo de planetapsilo."
   // Astro emits dist/404.html which GitHub Pages serves automatically for unmatched routes.

   import BaseLayout from '../layouts/BaseLayout.astro';

   const baseUrl = import.meta.env.BASE_URL;
   const homeHref = baseUrl; // "/planetapsilo/"
   ---
   <BaseLayout title="Te perdiste en el universo" description="404 — la ruta que buscás no existe en planetapsilo.">
     <section class="not-found">
       <div class="hero-bg" aria-hidden="true"></div>
       <div class="noise-overlay" aria-hidden="true"></div>
       <div class="container nf-content">
         <p class="kicker">404</p>
         <h1>Te perdiste en el universo de planetapsilo.</h1>
         <p class="lede">Esta ruta no existe — al menos no todavía.</p>
         <a class="cta-back" href={homeHref}>Volvé al inicio →</a>
       </div>
     </section>
   </BaseLayout>

   <style>
     .not-found {
       position: relative;
       isolation: isolate;
       min-height: 80vh;
       display: flex;
       align-items: center;
       overflow: hidden;
     }
     .hero-bg { position: absolute; inset: -10%; z-index: -2; opacity: 0.7; }
     .nf-content { text-align: center; position: relative; z-index: 1; padding-block: var(--space-section); }
     .kicker {
       font-family: var(--font-display);
       font-weight: 300;
       font-size: 1rem;
       letter-spacing: 0.32em;
       opacity: 0.6;
       margin-bottom: 1.5rem;
     }
     h1 {
       font-family: var(--font-display);
       font-weight: 300;
       font-size: clamp(1.75rem, 4vw, 2.75rem);
       max-width: 30rem;
       margin: 0 auto 1.5rem;
       line-height: 1.2;
     }
     .lede { color: var(--color-text-muted); max-width: 28rem; margin: 0 auto 2.5rem; }
     .cta-back {
       display: inline-block;
       padding: 0.875rem 1.75rem;
       border-radius: 0.5rem;
       background: var(--color-orange-mystic);
       color: var(--color-bg-base);
       font-family: var(--font-body);
       font-weight: 500;
     }
   </style>
   ```

6. Build and verify no errors:
   ```bash
   npm run build
   ```

7. Verify the built HTML files contain noindex + base-prefixed assets:
   ```bash
   # noindex on every page
   for f in dist/index.html dist/acompanamiento/index.html dist/retiros/index.html dist/contacto/index.html dist/404.html; do
     test -f "$f" && grep -q "noindex" "$f" && echo "OK noindex: $f" || echo "FAIL: $f"
   done

   # assets prefixed with /planetapsilo/
   grep -o '/planetapsilo/_astro/[^"]*' dist/index.html | head -3
   ```
  </action>
  <verify>
    <automated>cd /home/plazasia/workspace/07_DIGITAL_GROWTH/clientes/planetapsilo && test -f src/pages/index.astro && test -f src/pages/acompanamiento.astro && test -f src/pages/retiros.astro && test -f src/pages/contacto.astro && test -f src/pages/404.astro && grep -q "import BaseLayout" src/pages/index.astro && grep -q "import BaseLayout" src/pages/acompanamiento.astro && grep -q "import BaseLayout" src/pages/retiros.astro && grep -q "import BaseLayout" src/pages/contacto.astro && grep -q "import BaseLayout" src/pages/404.astro && grep -q "hero-bg" src/pages/index.astro && grep -q "Te perdiste" src/pages/404.astro && grep -q "Preparación" src/pages/retiros.astro && grep -q "Integración" src/pages/retiros.astro && npm run build 2>&1 | tail -20 | grep -E "(Complete|built in|✓)" && test -f dist/index.html && test -f dist/acompanamiento/index.html && test -f dist/retiros/index.html && test -f dist/contacto/index.html && test -f dist/404.html && grep -q "noindex" dist/index.html && grep -q "noindex" dist/acompanamiento/index.html && grep -q "noindex" dist/retiros/index.html && grep -q "noindex" dist/contacto/index.html && grep -q "noindex" dist/404.html && grep -q "/planetapsilo/_astro/" dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - 5 page files exist: `src/pages/{index,acompanamiento,retiros,contacto,404}.astro`
    - Each page imports BaseLayout (grep verified)
    - `index.astro` uses the `.hero-bg` cosmic-gradient class from tokens.css (D-04)
    - `404.astro` contains the literal phrase "Te perdiste" (cósmico-amable tone, CONTEXT.md Specifics)
    - `retiros.astro` mentions the three phases: Preparación, Inmersión, Integración
    - `contacto.astro` references all 3 stub CTAs (Calendly placeholder, WhatsApp stub, form stub)
    - `npm run build` exits 0
    - `dist/{index,acompanamiento/index,retiros/index,contacto/index,404}.html` all exist
    - Every built HTML contains the literal string `noindex`
    - Built HTML references assets via `/planetapsilo/_astro/` (Pitfall #13 verified end-to-end)
  </acceptance_criteria>
  <done>
The 4 pages + 404 are composed against BaseLayout, build green, ship `noindex` on every route, and reference assets under the correct base path. Plan 05 can now run the workflow + force-push and expect a working site at the public URL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Built HTML → GitHub Pages CDN → public visitor | Every meta tag, link, and asset path is baked at build time; misconfig is publicly visible. |
| `<a target="_blank">` (WhatsApp) → external WhatsApp domain | `rel="noopener noreferrer"` prevents `window.opener` tabnabbing. |
| Visitor's browser → Google Fonts CSS2 (loaded by global.css from Plan 02) | Third-party network call; documented + accepted (T-01-06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Information Disclosure | Page indexed by Google before legal pass (Pitfall #22) | mitigate | BaseLayout emits `<meta name="robots" content="noindex,nofollow">` while `site.indexable === false`; `public/robots.txt` declares `Disallow: /` (Task 1). Both gates enforced; flipping requires editing both. Acceptance criteria greps for `noindex` in EVERY built HTML. |
| T-01-17 | Tampering | Internal link uses hardcoded `/foo` and 404s on GH Pages (Pitfall #13) | mitigate | Nav + 404 + all pages use `import.meta.env.BASE_URL`; acceptance criteria greps for `import.meta.env.BASE_URL` in Nav.astro and built HTML for `/planetapsilo/` prefix. |
| T-01-18 | Tampering / Spoofing | External `target="_blank"` link enables tabnabbing | mitigate | WhatsAppFloat + Contacto WhatsApp CTA both set `rel="noopener noreferrer"` — acceptance criteria greps for both. |
| T-01-19 | Information Disclosure | Sofía's real WhatsApp / email exposed in stub | mitigate | Phase 1 stubs use `0000000000` and Calendly `#`; CONTEXT.md Deferred section locks this; Task 1 acceptance criteria greps `0000000000`. |
| T-01-20 | Denial of Service (accessibility) | Hero animation causes vestibular harm to reduced-motion users | mitigate | `.hero-bg` declares `animation: none` under `@media (prefers-reduced-motion: reduce)` (Plan 02 tokens.css); Task 2 and Task 3 do not override. Verified by grep in Plan 02 acceptance criteria. |
| T-01-21 | Information Disclosure | OG image leaks placeholder copy | accept | OG defaults are clearly placeholder ("Sitio en construcción"); Phase 2 will replace per page (CONT-12). |
</threat_model>

<verification>
End-of-plan checks:
1. `npm run build` exits 0.
2. `dist/` contains: `index.html`, `acompanamiento/index.html`, `retiros/index.html`, `contacto/index.html`, `404.html`, `favicon.svg`, `robots.txt`, `og-default.svg`.
3. `grep -l noindex dist/index.html dist/acompanamiento/index.html dist/retiros/index.html dist/contacto/index.html dist/404.html` lists all five (FOUND-08).
4. `grep "/planetapsilo/_astro/" dist/index.html` returns hits (Pitfall #13).
5. `grep "Disallow: /" dist/robots.txt` matches (FOUND-08).
6. `grep -q "radialGradient" dist/favicon.svg` (FOUND-03 favicon).
7. `npx astro preview --port 4321` serves under the base path; `curl -s http://localhost:4321/planetapsilo/` returns HTML containing "planetapsilo" (sanity).
</verification>

<success_criteria>
1. BaseLayout authored with full `<head>` (title, meta description, noindex, OG defaults, favicon, preconnect to Google Fonts).
2. Chrome components live: Nav (sticky + blur-on-scroll + mobile hamburger), Footer (brand line + noindex status), WhatsAppFloat (stub button with target/rel/aria).
3. Four page stubs + 404 build to valid HTML referencing the correct base path.
4. `public/favicon.svg`, `public/og-default.svg`, `public/robots.txt` in place.
5. `src/data/*.ts` typed and consumable.
6. The skeleton is visually coherent at `http://localhost:4321/planetapsilo/` via `astro preview`.
7. All 6 requirements addressed: FOUND-03 (BaseLayout), FOUND-05 (4 page stubs), FOUND-06 (Nav/Footer/WhatsAppFloat), FOUND-07 (data files), FOUND-08 (noindex meta + robots.txt), FOUND-09 (custom 404).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deployable-skeleton/01-04-SUMMARY.md` documenting:
- File tree under `src/` and `public/` after this plan
- Confirmation grep results: noindex present on all 5 built HTML files
- Confirmation that all internal links use `import.meta.env.BASE_URL`
- Locally-served preview URL + sanity-check that the wordmark renders and the cosmic gradient is visible
- Any deviation from the planned files_modified list
</output>
