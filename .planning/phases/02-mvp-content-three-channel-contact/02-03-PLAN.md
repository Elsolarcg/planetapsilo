---
phase: 02-mvp-content-three-channel-contact
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/data/services.ts
  - src/pages/acompanamiento.astro
autonomous: true
requirements:
  - CONT-02
  - CONT-09
  - CONT-12
must_haves:
  truths:
    - "Visitor on /planetapsilo/acompanamiento sees interior hero (opacity 0.5) with H1 'Acompañamiento' + 1-line lead"
    - "Visitor sees 'Para quién' block describing ICP (C-Level, founder, nómada digital) in legal-safe registry — no clinical language"
    - "Visitor sees 'Qué pasa en un encuentro' block with 3-4 bullets, list marker color orange-mystic"
    - "Visitor sees 'Cómo funciona el proceso' block with 3-step numbered list, each with Fraunces 500 display-md subhead"
    - "Visitor sees acompanamiento-context Testimonial card + LOCKED disclaimer"
    - "Visitor sees LOCKED ConfidentialityLine: 'Lo que se conversa aquí no sale de aquí. Trabajamos con discreción profesional y archivamos sólo lo mínimo necesario.'"
    - "Visitor sees final CTABlock (Calendly primary, WhatsApp secondary with prefill 'me interesa explorar el acompañamiento', tertiary form link)"
    - "Per-page meta title 'Acompañamiento — planetapsilo' and locked description"
  artifacts:
    - path: "src/pages/acompanamiento.astro"
      provides: "Full /acompanamiento page composing 7 sections per UI-SPEC"
      min_lines: 80
    - path: "src/data/services.ts"
      provides: "Extended Acompañamiento entry with description/audience/outcomes/confidentialityLine fields"
      contains: "audience:"
  key_links:
    - from: "src/pages/acompanamiento.astro"
      to: "src/components/sections/Hero.astro + Testimonial + ConfidentialityLine + CTABlock"
      via: "Astro frontmatter imports"
      pattern: "import .* from '\\.\\./components/sections/"
    - from: "src/pages/acompanamiento.astro"
      to: "src/data/testimonials.ts"
      via: "find(t => t.context === 'acompanamiento')"
      pattern: "context === 'acompanamiento'"
---

<objective>
Rewrite `src/pages/acompanamiento.astro` as a 7-section composition: interior Hero → Para quién → Qué pasa en un encuentro → Cómo funciona el proceso → Testimonial (acompanamiento context) → ConfidentialityLine → CTABlock. Extend `services.ts` Acompañamiento entry with description/audience/outcomes if the page needs them.

Purpose: First interior page consumes Plan 02's section library. Validates Hero variant="interior" + Testimonial context filter + CTABlock secondary prefill rotation (D-19).

Output: Complete `/acompanamiento` page with LOCKED legal-safe copy from Sofía's brand voice, zero glosario PROHIBIDO matches (denial clauses excepted per Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-content-three-channel-contact/02-CONTEXT.md
@.planning/phases/02-mvp-content-three-channel-contact/02-UI-SPEC.md
@.planning/phases/02-mvp-content-three-channel-contact/02-PATTERNS.md
@docs/copy-glossary.md
@src/components/sections/Hero.astro
@src/components/sections/Testimonial.astro
@src/components/sections/ConfidentialityLine.astro
@src/components/sections/CTABlock.astro
@src/data/testimonials.ts
@src/data/services.ts
@src/data/contact.ts
@src/pages/acompanamiento.astro
@src/styles/tokens.css

<interfaces>
From Plan 02:
- `Hero` accepts `{variant: 'home'|'interior', kicker?, title, maxim?, lead?}` and a `<slot />` for content under the lead
- `Testimonial` accepts `{quote, attribution, disclaimer?}`
- `ConfidentialityLine` accepts `{text: string}`
- `CTABlock` accepts `{primary: CTA, secondary?: CTA, tertiary?: CTA}` where `CTA = {label, href, external?, ariaLabel?}`

From Plan 01:
- `contact.calendlyUrl`, `waLink(prefill)`

From `src/data/testimonials.ts` (Plan 02 created):
- `testimonials.find(t => t.context === 'acompanamiento')` returns the D., C-Level retail testimonial

From `src/data/services.ts` (Plan 02 extended):
```ts
export interface Service {
  slug: 'acompanamiento' | 'retiros';
  label: string;
  teaser: string;
  ctaLabel: string;
  description?: string;
  audience?: string;
  outcomes?: string[];
  confidentialityLine?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend services.ts Acompañamiento entry with description/audience/outcomes/confidentiality</name>
  <files>src/data/services.ts</files>
  <read_first>
    - src/data/services.ts (Plan 02 created — currently has Service interface + 2 entries with only teaser+ctaLabel)
    - .planning/phases/02-mvp-content-three-channel-contact/02-CONTEXT.md §"Estructura propuesta de /acompanamiento" (bullets for "Para quién", "Qué pasa", "Cómo funciona"), §D-12 testimonial alignment
    - .planning/phases/02-mvp-content-three-channel-contact/02-UI-SPEC.md §"/acompanamiento" sections 2–4 (Para quién, Qué pasa, Cómo funciona)
    - docs/copy-glossary.md PERMITIDO list ("guía", "anfitriona", "acompañamiento", "encuentros", "espacio de exploración", "claridad", "expansión de consciencia") + PROHIBIDO list (zero "terapia", "tratamiento", "paciente", etc. except in denial-clause "no es terapia")
  </read_first>
  <behavior>
    - `services.find(s => s.slug === 'acompanamiento').audience` returns Spanish description of ICP (C-Level, founder, nómada digital, momento de inflexión)
    - `.description` returns 2-3 sentences placed in the page hero lead OR Para quién block
    - `.outcomes` returns array of 3-4 bullet strings describing "Qué pasa en un encuentro" — each in legal-safe registry
    - `.confidentialityLine` returns the LOCKED D-27 string
  </behavior>
  <action>
    REPLACE `src/data/services.ts` (full file replacement to keep both services in lockstep):

    ```ts
    // src/data/services.ts
    // Phase 2 (CONT-02, CONT-03, CONT-06, CONT-09): expanded entries.
    // Copy authored within docs/copy-glossary.md PERMITIDO. Denial-clause "no es terapia" appears
    // intentionally and is exempted by Plan 06 copy linter.
    // confidentialityLine matches D-27 LOCKED string.

    export interface Service {
      slug: 'acompanamiento' | 'retiros';
      label: string;
      teaser: string;
      ctaLabel: string;
      description?: string;
      audience?: string;
      outcomes?: string[];
      confidentialityLine?: string;
    }

    const CONFIDENTIALITY = 'Lo que se conversa aquí no sale de aquí. Trabajamos con discreción profesional y archivamos sólo lo mínimo necesario.';

    export const services: Service[] = [
      {
        slug: 'acompanamiento',
        label: 'Acompañamiento',
        teaser: 'Encuentros uno-a-uno para C-Level, founders y nómadas digitales que están reordenando algo importante. Espacio de exploración no clínico.',
        ctaLabel: 'Conocer el acompañamiento',
        description: 'Conversaciones sostenidas para mentes que ya construyeron mucho y ahora necesitan ver desde otra altura. No es terapia ni servicio de salud — es un espacio de exploración para reordenar decisiones, prioridades y dirección desde un lugar más quieto.',
        audience: 'Pensado para C-Level que están negociando una venta, una salida o un pivote; founders que sienten que el problema dejó de ser de equipo; y nómadas digitales que llevan años en piloto automático y empiezan a notar el costo.',
        outcomes: [
          'Una conversación uno-a-uno en un espacio sin interrupciones, sin agenda externa, sin protocolo clínico.',
          'Encuentros recurrentes — semanales, quincenales o mensuales — que sostienen un proceso en tu vida cotidiana, no un evento aislado.',
          'Trabajo desde la pregunta, no desde la receta. Lo que aparece se explora; lo que se ordena se ordena.',
          'Sin diagnóstico, sin etiquetas, sin recetario. Tu lenguaje, tu ritmo, tus decisiones.',
        ],
        confidentialityLine: CONFIDENTIALITY,
      },
      {
        slug: 'retiros',
        label: 'Retiros',
        teaser: 'Tres fases — Preparación, Inmersión, Integración — para una pausa profunda. Aplicación previa obligatoria.',
        ctaLabel: 'Conocer los retiros',
        // Plan 04 expands retiros fields (phases description, suitability, etc.) — left empty here intentionally.
        confidentialityLine: CONFIDENTIALITY,
      },
    ];
    ```
  </action>
  <verify>
    <automated>grep -q "audience:" src/data/services.ts && grep -q "outcomes:" src/data/services.ts && grep -q "description:" src/data/services.ts && grep -q "Lo que se conversa aquí no sale de aquí." src/data/services.ts && grep -q "No es terapia ni servicio de salud" src/data/services.ts && grep -c "ctaLabel:" src/data/services.ts && [ "$(grep -c 'ctaLabel:' src/data/services.ts)" = "2" ] && npm run build && grep -q "Acompañamiento y retiros para liderazgos que están reordenando." dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "audience:|outcomes:|description:" src/data/services.ts | wc -l` returns >= 3 lines (audience, outcomes, description present)
    - `grep -c "ctaLabel:" src/data/services.ts` returns 2 (unchanged from Plan 02)
    - `grep -q "Lo que se conversa aquí no sale de aquí" src/data/services.ts` matches
    - `npm run build` exits 0
    - Home page (dist/index.html) still contains `Conocer el acompañamiento` (regression check — Plan 02 still works)
  </acceptance_criteria>
  <done>Acompañamiento data ready for page composition. Retiros confidentialityLine field set; Plan 04 will fill the rest.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite src/pages/acompanamiento.astro composing 7 sections with LOCKED copy</name>
  <files>src/pages/acompanamiento.astro</files>
  <read_first>
    - src/pages/acompanamiento.astro (current Phase 1 stub — to REPLACE)
    - .planning/phases/02-mvp-content-three-channel-contact/02-UI-SPEC.md §"/acompanamiento — 7 sections" lines 67–74, §"Per-page meta" `/acompanamiento` row (line 475), §"Secondary CTA" `/acompanamiento` CTABlock row (prefill text), §"Typography" (H2 weight 300 / sub-headings + bullets specs), §"Color" §"Reserved for" rule #4 (orange-mystic list marker color)
    - .planning/phases/02-mvp-content-three-channel-contact/02-CONTEXT.md §"Estructura propuesta de /acompanamiento" (numbered structure 1–7), §D-19 (WhatsApp prefill rotation for acompañamiento context: "Hola planetapsilo, me interesa explorar el acompañamiento"), §D-27 (confidentiality)
    - src/data/services.ts (Task 1 — read description/audience/outcomes/confidentialityLine for Acompañamiento)
    - src/data/testimonials.ts (Plan 02 — has acompanamiento context entry)
  </read_first>
  <behavior>
    - GET /planetapsilo/acompanamiento/ returns 200
    - title = `Acompañamiento — planetapsilo`, description per UI-SPEC LOCKED
    - Section 1 (Hero interior): H1 "Acompañamiento" + lead body-lg 19px muted from services.description (truncated to 1 sentence)
    - Section 2 (Para quién): H2 weight 300 display-lg "Para quién" + paragraph from services.audience
    - Section 3 (Qué pasa en un encuentro): H2 weight 300 display-lg + unordered list mapping services.outcomes — each <li> marker color orange-mystic
    - Section 4 (Cómo funciona el proceso): H2 weight 300 display-lg + 3-step numbered list with subhead Fraunces 500 display-md + 1-2 lines body — Claude writes 3 steps within glosario PERMITIDO
    - Section 5 (Testimonial): testimonials.find(t => t.context === 'acompanamiento')
    - Section 6 (ConfidentialityLine): LOCKED D-27 text
    - Section 7 (CTABlock): primary Calendly external, secondary WhatsApp `waLink('Hola planetapsilo, me interesa explorar el acompañamiento')` external, tertiary "Prefiero escribir un formulario" → /contacto#form
    - noindex preserved via BaseLayout
  </behavior>
  <action>
    REPLACE `src/pages/acompanamiento.astro` with:

    ```astro
    ---
    // src/pages/acompanamiento.astro — Phase 2 (CONT-02, CONT-09, CONT-12)
    // Section order LOCKED in UI-SPEC §"/acompanamiento — 7 sections":
    //   1 Hero interior · 2 Para quién · 3 Qué pasa en un encuentro · 4 Cómo funciona el proceso
    //   · 5 Testimonial · 6 ConfidentialityLine · 7 CTABlock
    // Copy within docs/copy-glossary.md PERMITIDO; denial-clauses ("no es terapia") exempted in Plan 06 linter.

    import BaseLayout from '../layouts/BaseLayout.astro';
    import Hero from '../components/sections/Hero.astro';
    import Testimonial from '../components/sections/Testimonial.astro';
    import ConfidentialityLine from '../components/sections/ConfidentialityLine.astro';
    import CTABlock from '../components/sections/CTABlock.astro';
    import { services } from '../data/services';
    import { testimonials } from '../data/testimonials';
    import { contact, waLink } from '../data/contact';

    const acomp = services.find(s => s.slug === 'acompanamiento')!;
    const testimonial = testimonials.find(t => t.context === 'acompanamiento');

    const baseUrl = import.meta.env.BASE_URL;
    const baseTrimmed = baseUrl.replace(/\/$/, '');
    const contactForm = `${baseTrimmed}/contacto#form`;

    // 3-step proceso — Claude v1 within glosario PERMITIDO; Sofía refines wording.
    const procesoSteps = [
      {
        name: 'Una conversación inicial',
        body: 'Antes de cualquier encuentro hay una conversación de 30 minutos para saber si lo que pides y lo que ofrecemos calzan. Sin compromiso.',
      },
      {
        name: 'Encuentros sostenidos',
        body: 'Si decidimos seguir, agendamos encuentros semanales, quincenales o mensuales según tu momento. Cada uno dura entre 60 y 90 minutos, online o presencial en Bogotá.',
      },
      {
        name: 'Revisión cada cierto tiempo',
        body: 'Cada pocos encuentros nos detenemos a mirar el proceso completo — qué cambió, qué sigue, qué dejó de tener sentido. Tú decides cuándo cierra.',
      },
    ];
    ---
    <BaseLayout
      title="Acompañamiento"
      description="Encuentros uno-a-uno para C-Level, founders y nómadas digitales que están reordenando algo importante. No es terapia — es acompañamiento no clínico."
    >
      <Hero
        variant="interior"
        title="Acompañamiento"
        lead="Conversaciones sostenidas para mentes que ya construyeron mucho y ahora necesitan ver desde otra altura."
      />

      <section class="content-section para-quien">
        <div class="container narrow">
          <h2>Para quién</h2>
          <p>{acomp.audience}</p>
        </div>
      </section>

      <section class="content-section que-pasa">
        <div class="container narrow">
          <h2>Qué pasa en un encuentro</h2>
          <ul class="bullets" role="list">
            {acomp.outcomes!.map(o => <li>{o}</li>)}
          </ul>
        </div>
      </section>

      <section class="content-section como-funciona">
        <div class="container narrow">
          <h2>Cómo funciona el proceso</h2>
          <ol class="steps">
            {procesoSteps.map((step, i) => (
              <li>
                <span class="step-num" aria-hidden="true">{i + 1}</span>
                <div class="step-body">
                  <h3>{step.name}</h3>
                  <p>{step.body}</p>
                </div>
              </li>
            ))}
          </ol>
        </div>
      </section>

      {testimonial && (
        <Testimonial
          quote={testimonial.quote}
          attribution={testimonial.attribution}
          disclaimer={testimonial.disclaimer}
        />
      )}

      <ConfidentialityLine text={acomp.confidentialityLine!} />

      <CTABlock
        primary={{
          label: 'Agenda una conversación',
          href: contact.calendlyUrl,
          external: true,
        }}
        secondary={{
          label: 'Escribir por WhatsApp',
          href: waLink('Hola planetapsilo, me interesa explorar el acompañamiento'),
          external: true,
          ariaLabel: 'Escribir a planetapsilo por WhatsApp sobre acompañamiento',
        }}
        tertiary={{
          label: 'Prefiero escribir un formulario',
          href: contactForm,
        }}
      />
    </BaseLayout>

    <style>
      .content-section { padding-block: var(--space-section); }
      .container.narrow {
        max-width: 42rem;
        margin-inline: auto;
      }
      h2 {
        font-family: var(--font-display);
        font-weight: 300;
        font-size: clamp(1.625rem, 4vw, 2rem); /* --font-display-lg */
        line-height: 1.2;
        letter-spacing: -0.005em;
        color: var(--color-text-primary);
        margin: 0 0 32px;
      }
      p {
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 1.0625rem; /* --font-body-md 17px */
        line-height: 1.65;
        color: var(--color-text-primary);
        margin: 0 0 24px;
      }
      p:last-child { margin-bottom: 0; }

      .bullets {
        list-style: none;
        padding-left: 0;
        margin: 0;
      }
      .bullets li {
        position: relative;
        padding-left: 28px;
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 1.0625rem;
        line-height: 1.65;
        color: var(--color-text-primary);
        margin-bottom: 16px;
      }
      .bullets li::before {
        content: '◆';
        position: absolute;
        left: 0;
        top: 0;
        color: var(--color-orange-mystic);
        font-size: 0.85em;
        line-height: inherit;
      }

      .steps {
        list-style: none;
        padding-left: 0;
        margin: 0;
        display: flex;
        flex-direction: column;
        gap: 32px;
      }
      .steps > li {
        display: grid;
        grid-template-columns: 48px 1fr;
        gap: 24px;
        align-items: start;
      }
      .step-num {
        font-family: var(--font-display);
        font-weight: 300;
        font-size: 2rem;
        line-height: 1;
        color: var(--color-orange-mystic);
      }
      .step-body h3 {
        font-family: var(--font-display);
        font-weight: 500;
        font-style: italic;
        font-size: clamp(1.25rem, 3vw, 1.875rem); /* --font-display-md */
        line-height: 1.3;
        color: var(--color-text-primary);
        margin: 0 0 8px;
      }
      .step-body p { margin: 0; }
    </style>
    ```

    Note: step subhead applies `font-style: italic` + display-md — UI-SPEC §"Typography" reserves display-md to italic-only roles (máxima, testimonial quote, confidentiality, numbered-step subhead). This is one of the 4 sanctioned roles.
  </action>
  <verify>
    <automated>grep -q "import Hero" src/pages/acompanamiento.astro && grep -q "import Testimonial" src/pages/acompanamiento.astro && grep -q "import ConfidentialityLine" src/pages/acompanamiento.astro && grep -q "import CTABlock" src/pages/acompanamiento.astro && grep -q "context === 'acompanamiento'" src/pages/acompanamiento.astro && grep -q "waLink('Hola planetapsilo, me interesa explorar el acompañamiento')" src/pages/acompanamiento.astro && grep -q "variant=\"interior\"" src/pages/acompanamiento.astro && grep -q "Para quién" src/pages/acompanamiento.astro && grep -q "Qué pasa en un encuentro" src/pages/acompanamiento.astro && grep -q "Cómo funciona el proceso" src/pages/acompanamiento.astro && npm run build && grep -q "Para quién" dist/acompanamiento/index.html && grep -q "Qué pasa en un encuentro" dist/acompanamiento/index.html && grep -q "Cómo funciona el proceso" dist/acompanamiento/index.html && grep -q "Lo que se conversa aquí no sale de aquí" dist/acompanamiento/index.html && grep -q "Acompañamiento — planetapsilo" dist/acompanamiento/index.html && grep -q "<meta name=\"robots\" content=\"noindex,nofollow\">" dist/acompanamiento/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `dist/acompanamiento/index.html` contains `<title>Acompañamiento — planetapsilo</title>`
    - `dist/acompanamiento/index.html` contains the LOCKED description string starting `Encuentros uno-a-uno para C-Level, founders y nómadas digitales`
    - `dist/acompanamiento/index.html` contains LOCKED confidentiality string: `Lo que se conversa aquí no sale de aquí. Trabajamos con discreción profesional y archivamos sólo lo mínimo necesario.`
    - `dist/acompanamiento/index.html` contains the acompanamiento testimonial attribution `D., C-Level retail, 47 — Medellín`
    - `dist/acompanamiento/index.html` contains the WhatsApp URL with `me%20interesa%20explorar%20el%20acompa` (URL-encoded prefill)
    - `dist/acompanamiento/index.html` contains `<h1>Acompañamiento</h1>` (exact H1)
    - `dist/acompanamiento/index.html` contains `<meta name="robots" content="noindex,nofollow">`
    - `grep -c "<h2" dist/acompanamiento/index.html` returns at least 3 (Para quién, Qué pasa, Cómo funciona)
    - `dist/acompanamiento/index.html` does NOT contain the words "paciente", "tratamiento" outside a denial clause (linter Plan 06 will enforce — Plan 03 just authors clean)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>/acompanamiento renders 7 LOCKED sections. CONT-02 (page content + audience), CONT-09 (confidentiality), CONT-12 (per-page meta) addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → built static HTML | Spanish copy hardcoded from CONTEXT.md + services.ts (data layer). No dynamic copy. |
| Browser → external CTAs (Calendly + WhatsApp) | Tabnabbing risk — handled via Button primitive (Plan 01) + inline rel attrs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Repudiation | Copy could rozar registro clínico ("paciente" / "tratamiento") | mitigate | Plan 02 + 03 author copy v1 within glosario; Plan 06 deploys CI linter to block any future regression. Denial-clauses pre-approved as exemptions. |
| T-02-10 | Tampering | WhatsApp prefill rotation per page (different message for acompanamiento context) | mitigate | `waLink()` from Plan 01 URL-encodes; prefill text is hardcoded literal in the page (not user-supplied). |
</threat_model>

<verification>
1. `npm run build` exits 0
2. `dist/acompanamiento/index.html` contains all 7 section headings + LOCKED testimonial + LOCKED confidentiality + final CTABlock with 3 buttons
3. Home page (`dist/index.html`) unaffected (regression check — Plan 02 still passes its verifications)
4. `prefers-reduced-motion` rules from Plan 02 components propagate to this page (they are scoped to the components, not the page)
</verification>

<success_criteria>
- /acompanamiento page renders 7 sections in LOCKED order from UI-SPEC
- Per-page meta correct
- Plan addresses CONT-02 (page content + audience + outcomes), CONT-09 (confidentiality line present), CONT-12 (acompanamiento per-page meta)
</success_criteria>

<output>
After completion, create `.planning/phases/02-mvp-content-three-channel-contact/02-03-SUMMARY.md` covering:
- Final acompanamiento page section order + LOCKED strings landed
- Open items for Sofía: testimonial wording refinement, 3 proceso steps wording refinement (v1 by Claude)
- Any divergences from UI-SPEC and why (none expected — flag if any)
</output>
