---
context: phase
phase: 02-mvp-content-three-channel-contact
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-05-24T01:47:37.016Z
---

# Critical Anti-Patterns

_No blocking anti-patterns were discovered this session. Section retained empty for future updates._

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| (none) | — | — | — |

<current_state>
Phase 02 is **content-and-gate-complete**. All 4 LOCKED pages (home, /acompanamiento, /retiros, /contacto) + /privacidad ship with locked copy, working forms, channel duo, application gate, footer disclaimer, and the copy-lint CI gate is wired.

Plan 02-07 Task 1 (automated audits) ran clean and was committed as `437cb78`. The executor paused at **Task 2 — Real-device CTA verification (CONT-16)**, a `checkpoint:human-verify gate="blocking"` task.

At the checkpoint, the user previously chose **"Test now against placeholders (visual only)"** — meaning the iPhone + Android visual CTA protocol runs against current `src/data/contact.ts` placeholders, and the form-submit + WhatsApp-prefill validation gets re-run after the Web3Forms keys are swapped.

**Session 2026-05-24 update:** the user reopened the project, ran a **local preview review** (`npm run preview --host 0.0.0.0 --port 4321`), visually confirmed the 4 LOCKED pages + /privacidad + /obras all render OK in their browser (all routes returned HTTP 200), then said _"todo bien pero vamos a seguir haciendo unos ajustes"_ and invoked `/gsd-pause-work` to end the day. The specific ajustes were NOT enumerated. Preview server was cleanly killed.

The working tree is clean. Resume by **asking the user to enumerate the ajustes first**, applying each as a discrete commit, then proceeding to Task 2 visual-only protocol → Task 3 → SUMMARY → Phase close.
</current_state>

<completed_work>

Plans completed (all atomic commits + SUMMARY.md per plan):

- **02-01** Foundation primitives — `a20345f` — contact.ts + waLink + Button + Footer + WhatsAppFloat + /privacidad
- **02-02** Section library + index — `0b23c77` — 8 section components + 3 data modules + home rewrite
- **02-03** /acompanamiento — `3d106e7` — 7-section composition + services.ts populated + denial clause embedded
- **02-04** /retiros + form library — `9dc2535` — FormField/HabeasDataCheckbox/FormSuccess + RetreatPhases + RetreatApplicationForm + page rewrite (D-07 honored: ZERO `calendly` matches in `dist/retiros/`)
- **02-05** /contacto — `610ab95` — ContactForm + 4-section composition with channel duo
- **02-06** Copy-lint CI gate — `6da0bec` — scripts/copy-lint.mjs + glossary §Exemptions + npm scripts + deploy.yml wiring (lint runs BEFORE upload)
- **02-07 Task 1** Automated audits — `437cb78` — 02-07-AUDIT.md created with 8 sections; all automated gates green

Session 2026-05-24 (informal, no commit):
- Local preview review on `http://localhost:4321/planetapsilo/` — user opened 4 LOCKED pages in browser + visually confirmed render. Not a formal Task 2 step.
</completed_work>

<remaining_work>

- **🔥 NEW — User-specified "ajustes" (blocking, list TBD):** user flagged adjustments on 2026-05-24 without enumerating. ASK on resume. Scope each as (a) copy tweak in `src/data/*.ts`, (b) component visual tweak, (c) structural page change → spawn `02-07.1-PLAN.md`, or (d) data swap (contact.ts placeholders).
- **Plan 02-07 Task 2 (in_progress, blocking checkpoint):** Real-device CTA verification — visual-only path (see Path A below). **Resume AFTER ajustes are applied** so devices test the final state, not the pre-ajustes state.
- **Plan 02-07 Task 3 (not started, blocking checkpoint):** Sofía walks through 4 pages and signs off / refines copy
- **Plan 02-07 SUMMARY.md** — written after Task 3 resumes
- **Phase 2 close routine** — code-review gate → regression gate → phase verifier → ROADMAP/STATE/PROJECT.md updates → offer next phase
- **Outside-of-checkpoint (pre-deploy gate):** swap real values for `whatsappNumber`, `web3formsKeyContacto`, `web3formsKeyRetiros`, and `calendlyUrl` in `src/data/contact.ts`; rerun the form-submit + WhatsApp-prefill iPhone steps after swap
</remaining_work>

<decisions_made>

- **Local preview render = OK (informal)** — session 2026-05-24, user visually confirmed all 4 LOCKED pages + /privacidad + /obras render correctly in their browser via `npm run preview --host`. Does NOT count as Task 2 completion (real-device protocol still pending). Recorded as informal confidence boost.
- **Visual-only Task 2 path** — at the Task 2 checkpoint user chose to defer form-submit + WhatsApp-prefill validation until real values are swapped. Visual proof (layout, /retiros NO Calendly, keyboard zoom, reduced-motion emulation) runs against placeholders now.
- **Worktrees disabled — sequential execution** — `workflow.use_worktrees=false`; all 7 plans ran one at a time on main working tree.
- **D-07 application gate honored** — zero `calendly` matches in `dist/retiros/index.html`; `/retiros` offers `RetreatApplicationForm` as the only conversion path.
- **Copy-lint gate runs BEFORE deploy** — T-02-20 mitigation in `.github/workflows/deploy.yml`; lint@line 46 < upload@line 54. Negative test (`Ofrecemos terapia`) confirmed exit 1.
- **Denial-clause exemption pattern formalized** — `EXEMPT_PATTERNS` in `scripts/copy-lint.mjs` ↔ `docs/copy-glossary.md §Exemptions` share one rulebook; 7 documented exempt patterns including "No es terapia ni servicio de salud".
</decisions_made>

<blockers>

- **🔥 Pending ajustes list from user** — user said _"vamos a seguir haciendo unos ajustes"_ on 2026-05-24 without specifying. Resume MUST start by asking what to adjust. Do NOT silently expand Plan 02-07 if ajustes are structural — spawn `02-07.1-PLAN.md` instead.

- **Three deploy-blocking placeholders in `src/data/contact.ts`:**
  - `whatsappNumber: '57XXXXXXXXXX'`
  - `web3formsKeyContacto: 'YOUR_CONTACTO_ACCESS_KEY'`
  - `web3formsKeyRetiros: 'YOUR_RETIROS_ACCESS_KEY'`

  Workaround: visual-only Task 2 path lets us proceed without these now. Real swap happens before live deploy; the iPhone step-7 (form submit) + step-2/5 (WhatsApp prefill) get re-run after.

- **Lighthouse mobile in sandbox = MANUAL** — no Chromium available in this environment. Recorded instructions in `02-07-AUDIT.md §Lighthouse mobile`. Run `npm run preview`, then DevTools Lighthouse mobile profile on the 4 pages locally.

- **Sofía availability** — Task 3 requires Sofía herself walking through the live URL (or local preview screen-recording). Blocked on her time.
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-mvp-content-three-channel-contact/02-07-PLAN.md` — Task 2 `<how-to-verify>` block has the canonical CTA protocol
2. `.planning/phases/02-mvp-content-three-channel-contact/02-07-AUDIT.md` — already has placeholder tables ready to fill under `## Real-device verification (Task 2)`
3. `.planning/phases/02-mvp-content-three-channel-contact/02-CONTEXT.md` §D-23 (Lighthouse target), §D-24 (reduced-motion), §D-25 (lint gate)
4. `.planning/phases/02-mvp-content-three-channel-contact/02-UI-SPEC.md` §Motion (lines 488–517) — DevTools reduced-motion checklist
5. `src/data/contact.ts` — 4 TODO placeholders (with explicit comments) to swap before pre-deploy

## Infrastructure State

- **Working tree:** clean before this WIP commit. All 7 plans + Task 1 audit committed.
- **Branch:** `main`. No remote push since execution began (deploy is GH Pages workflow-driven; user pushes when ready).
- **`npm run build:check`:** exits 0 as of commit `437cb78` — 7 pages built, copy-lint green, zero non-exempt PROHIBIDO matches.
- **Background processes:** none. Preview server (`npm run preview`) from session 2026-05-24 was cleanly killed (SIGTERM, exit 143). Port 4321 free.
- **Live URL:** `https://elsolarcg.github.io/planetapsilo/` — still serving the prior commit on GH Pages (Phase 1 + /obras stub). Phase 2 content lands when user pushes.

## Next Action — Resume Order

### Step 0 (FIRST THING) — Get the ajustes list

Ask the user: **"Antes de retomar Task 2, ¿qué ajustes quieres hacer? Lista cada uno y clasificamos: (a) copy en `src/data/*.ts`, (b) tweak visual de un componente, (c) cambio estructural de página → spawn `02-07.1-PLAN.md`, (d) swap de placeholders en `contact.ts`."**

Apply each ajuste as its own atomic commit. Re-run `npm run build:check` after each commit. Do not silently expand Plan 02-07.

### Step 1 — Resume Task 2 (visual-only)

After ajustes are in:

1. Open `https://elsolarcg.github.io/planetapsilo/` on iPhone + Android (or `npm run preview` on a phone-shaped browser frame for a local-only test).
2. Walk through the 8 iPhone steps + 3 Android steps from `02-07-PLAN.md` Task 2 `<how-to-verify>`. Visual-only path — **skip step 7 (form submit) and step 8 (Web3Forms inbox)**; they get re-run after `contact.ts` swap.
3. Open DevTools → Rendering → Emulate `prefers-reduced-motion: reduce`. Verify: (a) `.hero-bg` frozen, (b) primary CTA hover no `scale(1.02)`, (c) FAQ chevron snaps instantly, (d) form spinner static.
4. `npm run build && npm run preview` → DevTools Lighthouse mobile profile on `/`, `/acompanamiento`, `/retiros`, `/contacto`. Record P/A/S scores. SEO will drop ~10pt from noindex — expected.
5. Append results to `.planning/phases/02-mvp-content-three-channel-contact/02-07-AUDIT.md` under `## Real-device verification (Task 2)` (placeholder tables are already there).
6. Reply `approved` in chat → orchestrator resumes Plan 02-07 Task 3.

### Step 2 — Task 3 (Sofía) → SUMMARY → Phase close

Per the original Path A in the previous handoff version.

<context>
You were running `/gsd-execute-phase 2`. The orchestrator ran all 6 waves sequentially (worktrees disabled) and reached the Plan 02-07 checkpoint. Task 1 ran automated; Tasks 2 + 3 are blocking human checkpoints. At the checkpoint prompt the user picked "Test now against placeholders (visual only)" — so the executor is now paused waiting for the user to come back with iPhone + Android visual test results.

The 2026-05-24 session was a brief local-preview review. The user did NOT execute the device protocol. They confirmed local render is OK and flagged unspecified ajustes for the next session. They did not provide the ajustes list before pausing.

Plan 02-07 is the last plan of Phase 2. After Tasks 2 + 3 resolve, Phase 2 close routine runs: code-review gate → regression gate → phase verifier → ROADMAP/STATE/PROJECT updates → routing to Phase 02.1 (Tienda Mínima) or Phase 3 (Legal Pass) depending on user preference.
</context>

<next_action>
Resume by asking the user to enumerate the "ajustes" they want to make before Task 2 (see Step 0 above). Apply each as a discrete atomic commit. Then resume the visual CTA protocol on iPhone + Android. Then reply `approved` (or describe failures) in chat to proceed to Task 3.
</next_action>
