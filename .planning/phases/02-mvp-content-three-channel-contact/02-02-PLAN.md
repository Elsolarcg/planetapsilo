---
phase: 02-mvp-content-three-channel-contact
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/sections/Hero.astro
  - src/components/sections/BrandBio.astro
  - src/components/sections/ServiceTeaser.astro
  - src/components/sections/ServicesSplit.astro
  - src/components/sections/Testimonial.astro
  - src/components/sections/FAQ.astro
  - src/components/sections/ConfidentialityLine.astro
  - src/components/sections/CTABlock.astro
  - src/data/testimonials.ts
  - src/data/faqs.ts
  - src/data/services.ts
  - src/pages/index.astro
autonomous: true
requirements:
  - CONT-01
  - CONT-06
  - CONT-07
  - CONT-08
  - CONT-09
  - CONT-12
must_haves:
  truths:
    - "Visitor on `/` sees: gradient hero with kicker 'planetapsilo' + H1 'Acompañamiento y retiros para liderazgos que están reordenando.' + máxima 'La claridad no se conquista. Se recuerda.' + dual CTA (Calendly primary + Explora los retiros secondary)"
    - "Visitor sees BrandBio 2-paragraph block 'Sobre planetapsilo' with the locked legal-safe copy"
    - "Visitor sees Services split with 2 cards: Acompañamiento → 'Conocer el acompañamiento' / Retiros → 'Conocer los retiros' linking to respective pages"
    - "Visitor sees 1 placeholder testimonial card (home variant) + disclaimer line"
    - "Visitor sees 6 FAQ accordion items, default closed, native <details><summary> (zero JS)"
    - "Visitor sees final CTABlock with primary (Calendly) + secondary (WhatsApp) + tertiary (form link to /contacto#form)"
    - "Per-page meta title/description matches UI-SPEC §Per-page meta exactly for home"
  artifacts:
    - path: "src/components/sections/Hero.astro"
      provides: "Hero component with variant home/interior, supports kicker/title/maxim/lead props"
      exports: ["default (Astro component)"]
    - path: "src/components/sections/BrandBio.astro"
      provides: "2-3 paragraph centered block"
    - path: "src/components/sections/ServiceTeaser.astro"
      provides: "Single card: title + teaser + CTA"
    - path: "src/components/sections/ServicesSplit.astro"
      provides: "Wraps 2 ServiceTeaser cards in responsive grid"
    - path: "src/components/sections/Testimonial.astro"
      provides: "Single quote card with attribution + disclaimer"
    - path: "src/components/sections/FAQ.astro"
      provides: "Native <details> accordion with locked styling"
    - path: "src/components/sections/ConfidentialityLine.astro"
      provides: "Centered italic one-liner (consumed in Plans 03 + 04)"
    - path: "src/components/sections/CTABlock.astro"
      provides: "Primary + Secondary + Tertiary CTA cluster composing Button primitive"
    - path: "src/data/testimonials.ts"
      provides: "3 placeholder testimonials (home / acompanamiento / retiros)"
      contains: "export const testimonials"
    - path: "src/data/faqs.ts"
      provides: "6 FAQ items"
      contains: "export const faqs"
    - path: "src/data/services.ts"
      provides: "Extended interface with ctaLabel per card (Conocer el acompañamiento / Conocer los retiros)"
    - path: "src/pages/index.astro"
      provides: "Composed home page using all section components above"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/sections/Hero.astro + BrandBio + ServicesSplit + Testimonial + FAQ + CTABlock"
      via: "Astro frontmatter imports + JSX composition"
      pattern: "import .* from '\\.\\./components/sections/"
    - from: "src/components/sections/ServicesSplit.astro"
      to: "src/data/services.ts"
      via: "Receives services prop or maps over imported services array"
      pattern: "services\\.map"
    - from: "src/components/sections/CTABlock.astro"
      to: "src/components/ui/Button.astro"
      via: "Composes 3 Button instances"
      pattern: "import Button from '\\.\\./ui/Button\\.astro'"
---

<objective>
Build the section component library (Hero, BrandBio, ServiceTeaser, ServicesSplit, Testimonial, FAQ, ConfidentialityLine, CTABlock) + the testimonials/faqs data modules, then compose `src/pages/index.astro` as the first page consuming them. After this plan, Plans 03–05 import the same section components for their pages.

Purpose: One plan introduces the entire section library so subsequent page plans (03–05) are pure composition — they pick components off the shelf instead of inventing them. ServicesSplit + FAQ + Testimonial only ever appear on home + sometimes other pages; the components must accept props so they can be reused.

Output: 8 new section components + 3 new/extended data files + the home page composed to UI-SPEC §"Per-page section composition `/` (home)" — 6 sections in the locked order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-content-three-channel-contact/02-CONTEXT.md
@.planning/phases/02-mvp-content-three-channel-contact/02-UI-SPEC.md
@.planning/phases/02-mvp-content-three-channel-contact/02-PATTERNS.md
@docs/copy-glossary.md
@src/layouts/BaseLayout.astro
@src/components/ui/Button.astro
@src/data/contact.ts
@src/data/services.ts
@src/data/nav.ts
@src/styles/tokens.css
@src/styles/global.css
@src/pages/index.astro
@src/pages/obras.astro

<interfaces>
Key types and components downstream depends on.

From Plan 01 — `src/components/ui/Button.astro`:
```ts
interface Props {
  variant: 'primary' | 'secondary' | 'tertiary';
  href: string;
  label: string;
  external?: boolean;
  ariaLabel?: string;
}
```

From Plan 01 — `src/data/contact.ts`:
```ts
export const contact: ContactConfig;  // includes calendlyUrl, instagramUrl, tiktokUrl, web3formsKey*, whatsappNumber
export const waLink = (prefill: string) => string;
```

From `src/data/services.ts` (current Phase 1 — to be EXTENDED in Task 8 below):
```ts
export interface ServicePlaceholder {
  slug: 'acompanamiento' | 'retiros';
  label: string;
  teaser: string;
}
export const services: ServicePlaceholder[];
```

From `src/data/nav.ts`:
```ts
export const nav: NavItem[];  // 5 items: Inicio / Acompañamiento / Retiros / Obras / Contacto
```

Reused pattern: BASE_URL `internal()` helper from `src/components/layout/Nav.astro` lines 10–17 — every internal `<a href>` in this plan's components MUST use it.

Card grid pattern from `src/pages/obras.astro` lines 188–202 (responsive grid 1col / 2col / 3col) is the analog for ServicesSplit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Hero + BrandBio + ConfidentialityLine + Testimonial components (presentational, no JS)</name>
  <files>
    src/components/sections/Hero.astro,
    src/components/sections/BrandBio.astro,
    src/components/sections/ConfidentialityLine.astro,
    src/components/sections/Testimonial.astro
  </files>
  <read_first>
    - .planning/phases/02-mvp-content-three-channel-contact/02-UI-SPEC.md §"Per-page section composition" (home + interior hero specs lines 60–88), §"Component Contracts" lines 218–234, §"Typography" lines 132–161
    - .planning/phases/02-mvp-content-three-channel-contact/02-PATTERNS.md §"Hero.astro" lines 197–278, §"Testimonial component" parts in §"ServiceTeaser + ServicesSplit + Testimonial + RetreatPhases" lines 281–328, §"ConfidentialityLine" implied via D-27
    - src/pages/index.astro (current Phase 1 inline hero, lines 13–25 markup + 28–74 styles — the EXTRACTION source)
    - src/pages/404.astro (for `.hero-bg` + `.noise-overlay` shell pattern)
    - .planning/phases/02-mvp-content-three-channel-contact/02-CONTEXT.md §D-04 (máxima anchor), §D-11 (testimonial format), §D-14 (BrandBio paragraphs), §D-27 (confidentiality line)
    - .planning/phases/02-mvp-content-three-channel-contact/02-UI-SPEC.md §"Locked strings" (BrandBio LOCKED paragraphs, Testimonial disclaimer LOCKED, Confidentiality one-liner LOCKED)
  </read_first>
  <behavior>
    - `<Hero variant="home" kicker="planetapsilo" title="..." maxim="..." />` renders hero with min-height 100vh max 720px + opacity 0.85 + kicker + H1 + máxima italic
    - `<Hero variant="interior" title="Acompañamiento" lead="..." />` renders hero with min-height 60vh max 520px + opacity 0.5 + H1 + lead body-lg muted (no kicker by default, no máxima)
    - `<BrandBio paragraphs={['p1', 'p2']} />` renders centered max-width 42rem block with 2-3 paragraphs at body-md 17px line-height 1.7
    - `<ConfidentialityLine text="Lo que se conversa aquí..." />` renders italic Fraunces 300 display-md centered muted, padding-block var(--space-section)
    - `<Testimonial quote="..." attribution="M., ..." disclaimer="Testimonio ilustrativo..." />` renders Fraunces italic quote + 14px attribution + 13px disclaimer in a single centered card
    - All components consume `var(--space-section)` for outer padding-block
    - All hover transitions wrapped in `prefers-reduced-motion: reduce` override
  </behavior>
  <action>
    Create directory `src/components/sections/` (if not present) and write 4 files.

    **FILE 1: `src/components/sections/Hero.astro`**

    ```astro
    ---
    // src/components/sections/Hero.astro
    // Phase 2 hero — extracted from Phase 1 inline pattern in src/pages/index.astro lines 13–74.
    // Variants: 'home' (min 100vh, opacity 0.85, máxima allowed) / 'interior' (min 60vh, opacity 0.5, no máxima).
    // Reduced-motion: .hero-bg animation is already disabled globally in tokens.css; no override needed here.

    interface Props {
      variant: 'home' | 'interior';
      kicker?: string;
      title: string;
      maxim?: string;
      lead?: string;
    }
    const { variant, kicker, title, maxim, lead } = Astro.props as Props;
    ---
    <section class:list={['hero', `hero--${variant}`]}>
      <div class="hero-bg" aria-hidden="true"></div>
      <div class="noise-overlay" aria-hidden="true"></div>
      <div class="container hero-content">
        {kicker && <p class="kicker">{kicker}</p>}
        <h1>{title}</h1>
        {maxim && <p class="maxim">{maxim}</p>}
        {lead && <p class="lead">{lead}</p>}
        <slot />
      </div>
    </section>

    <style>
      .hero {
        position: relative;
        isolation: isolate;
        display: flex;
        align-items: center;
        overflow: hidden;
      }
      .hero--home    { min-height: min(100vh, 720px); }
      .hero--interior{ min-height: min(60vh, 520px); }
      .hero-bg {
        position: absolute;
        inset: -10%;
        z-index: -2;
      }
      .hero--home .hero-bg     { opacity: 0.85; }
      .hero--interior .hero-bg { opacity: 0.5; }

      .hero-content {
        position: relative;
        z-index: 1;
        text-align: center;
        padding-block: var(--space-section);
      }

      .kicker {
        font-family: var(--font-display);
        font-weight: 300;
        font-size: 1.125rem; /* --font-display-sm 18px */
        letter-spacing: 0.16em;
        text-transform: lowercase;
        color: var(--color-text-primary);
        opacity: 0.85;
        margin: 0 0 24px;
      }

      h1 {
        font-family: var(--font-display);
        font-weight: 300;
        font-size: clamp(2rem, 5.5vw, 4.5rem); /* --font-display-xl */
        line-height: 1.1;
        letter-spacing: -0.01em;
        color: var(--color-text-primary);
        margin: 0 0 24px;
        max-width: 32rem;
        margin-inline: auto;
      }

      .maxim {
        font-family: var(--font-display);
        font-weight: 300;
        font-style: italic;
        font-size: clamp(1.25rem, 3vw, 1.875rem); /* --font-display-md */
        line-height: 1.3;
        letter-spacing: -0.005em;
        color: var(--color-text-muted);
        margin: 0 auto 32px;
        max-width: 28rem;
      }

      .lead {
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 1.1875rem; /* --font-body-lg 19px */
        line-height: 1.5;
        color: var(--color-text-muted);
        margin: 0 auto;
        max-width: 36rem;
      }
    </style>
    ```

    **FILE 2: `src/components/sections/BrandBio.astro`**

    ```astro
    ---
    // src/components/sections/BrandBio.astro
    // Phase 2 D-14: "Sobre planetapsilo" — 2-3 paragraph centered block (no images).
    // Copy LOCKED in UI-SPEC §"Locked strings" rows "Brand bio paragraph 1/2/3".
    // Paragraph 1 + 2 contain denial-clause "No es terapia" — Plan 06 lint exemption handles.

    interface Props {
      paragraphs: string[];
    }
    const { paragraphs } = Astro.props as Props;
    ---
    <section class="brand-bio">
      <div class="container bb-content">
        {paragraphs.map(p => <p>{p}</p>)}
      </div>
    </section>

    <style>
      .brand-bio { padding-block: var(--space-section); }
      .bb-content {
        max-width: 42rem;
        margin-inline: auto;
        text-align: center;
      }
      p {
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 1.0625rem; /* --font-body-md 17px */
        line-height: 1.7;
        color: var(--color-text-primary);
        margin: 0 0 24px;
      }
      p:last-child { margin-bottom: 0; }
    </style>
    ```

    **FILE 3: `src/components/sections/ConfidentialityLine.astro`**

    ```astro
    ---
    // src/components/sections/ConfidentialityLine.astro
    // Phase 2 D-27: single confidentiality sentence in display-md italic centered (CONT-09).
    // LOCKED copy: "Lo que se conversa aquí no sale de aquí. Trabajamos con discreción profesional y archivamos sólo lo mínimo necesario."

    interface Props {
      text: string;
    }
    const { text } = Astro.props as Props;
    ---
    <section class="confidentiality">
      <div class="container">
        <p>{text}</p>
      </div>
    </section>

    <style>
      .confidentiality { padding-block: var(--space-section); }
      p {
        font-family: var(--font-display);
        font-weight: 300;
        font-style: italic;
        font-size: clamp(1.25rem, 3vw, 1.875rem); /* --font-display-md */
        line-height: 1.3;
        text-align: center;
        color: var(--color-text-muted);
        margin: 0 auto;
        max-width: 36rem;
      }
    </style>
    ```

    **FILE 4: `src/components/sections/Testimonial.astro`**

    ```astro
    ---
    // src/components/sections/Testimonial.astro
    // Phase 2 D-11 / D-12 / CONT-07: 1 testimonial card per page (rotation by context).
    // Quote in Fraunces italic display-md (matches máxima / confidentiality typography role).
    // Disclaimer required while testimonials are placeholders (Phase 3 LEGAL-07 replaces with real consented).

    interface Props {
      quote: string;
      attribution: string;
      disclaimer?: string;
    }
    const { quote, attribution, disclaimer } = Astro.props as Props;
    ---
    <section class="testimonial">
      <div class="container">
        <figure>
          <blockquote class="quote">
            <p>“{quote}”</p>
          </blockquote>
          <figcaption>
            <p class="attribution">{attribution}</p>
            {disclaimer && <p class="disclaimer">{disclaimer}</p>}
          </figcaption>
        </figure>
      </div>
    </section>

    <style>
      .testimonial { padding-block: var(--space-section); }
      figure {
        max-width: 36rem;
        margin: 0 auto;
        text-align: center;
      }
      .quote { margin: 0; }
      .quote p {
        font-family: var(--font-display);
        font-weight: 300;
        font-style: italic;
        font-size: clamp(1.25rem, 3vw, 1.875rem); /* --font-display-md */
        line-height: 1.3;
        color: var(--color-text-primary);
        margin: 0;
      }
      figcaption {
        margin-top: 24px;
      }
      .attribution {
        font-family: var(--font-body);
        font-weight: 500;
        font-size: 0.875rem;  /* --font-body-sm 14px */
        line-height: 1.5;
        letter-spacing: 0.02em;
        color: var(--color-text-primary);
        margin: 0;
      }
      .disclaimer {
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 0.8125rem; /* --font-body-xs 13px */
        line-height: 1.5;
        color: var(--color-text-subtle);
        margin: 16px auto 0;
        max-width: 30rem;
      }
    </style>
    ```
  </action>
  <verify>
    <automated>test -f src/components/sections/Hero.astro && test -f src/components/sections/BrandBio.astro && test -f src/components/sections/ConfidentialityLine.astro && test -f src/components/sections/Testimonial.astro && grep -q "variant: 'home' | 'interior'" src/components/sections/Hero.astro && grep -q "min-height: min(100vh, 720px)" src/components/sections/Hero.astro && grep -q "min-height: min(60vh, 520px)" src/components/sections/Hero.astro && grep -q "opacity: 0.85" src/components/sections/Hero.astro && grep -q "opacity: 0.5" src/components/sections/Hero.astro && grep -q "font-style: italic" src/components/sections/Testimonial.astro && grep -q "font-style: italic" src/components/sections/ConfidentialityLine.astro && grep -q "font-style: italic" src/components/sections/Hero.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 4 component files exist under `src/components/sections/`
    - `grep -c "font-style: italic" src/components/sections/Hero.astro` returns 1 (the maxim class)
    - `grep -c "padding-block: var(--space-section)" src/components/sections/{BrandBio,ConfidentialityLine,Testimonial}.astro` returns 3
    - Hero variant CSS: both `.hero--home` and `.hero--interior` blocks present
    - Testimonial has `<figure><blockquote>` semantic structure (a11y)
    - `npm run build` exits 0
    - No new lint violations introduced (npm run build does not emit warnings)
  </acceptance_criteria>
  <done>4 presentational section components ready for home + (later) interior page composition.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ServiceTeaser + ServicesSplit + CTABlock + FAQ components + extend services.ts</name>
  <files>
    src/components/sections/ServiceTeaser.astro,
    src/components/sections/ServicesSplit.astro,
    src/components/sections/CTABlock.astro,
    src/components/sections/FAQ.astro,
    src/data/services.ts,
    src/data/faqs.ts
  </files>
  <read_first>
    - .planning/phases/02-mvp-content-three-channel-contact/02-UI-SPEC.md §"Component Contracts" lines 218–234, §"Per-page section composition" home steps 3, 5, 6, §"Accordion (FAQ) interaction" lines 327–333, §"Locked strings" rows "ServiceTeaser CTA — Acompañamiento card" + "ServiceTeaser CTA — Retiros card", §"Color" §"Reserved for" rule #4 (list marker color)
    - .planning/phases/02-mvp-content-three-channel-contact/02-CONTEXT.md §D-05 (3-level CTA jerarquía), §D-13 (6 FAQ questions LOCKED list)
    - .planning/phases/02-mvp-content-three-channel-contact/02-PATTERNS.md §"ServiceTeaser + ServicesSplit + Testimonial + RetreatPhases" lines 281–328, §"FAQ.astro" lines 333–375, §"CTABlock.astro" lines 378–423, §"src/data/services.ts (modified)", §"src/data/faqs.ts (NEW)"
    - src/pages/obras.astro lines 188–202 (responsive grid analog — 1col mobile / 2col tablet / 3col desktop)
    - src/data/services.ts (current Phase 1 shape — to extend with ctaLabel)
    - src/components/ui/Button.astro (from Plan 01 — CTABlock consumes 3 of these)
  </read_first>
  <behavior>
    - `<ServiceTeaser title="Acompañamiento" teaser="..." ctaLabel="Conocer el acompañamiento" ctaHref="/acompanamiento" />` renders card with H3 title (Fraunces 500 display-lg) + 1-line teaser + tertiary CTA via Button
    - `<ServicesSplit services={services} />` renders 2 ServiceTeaser cards in grid: 1col mobile, 2col tablet+
    - `<CTABlock primary={{label,href,external?}} secondary={{...}} tertiary={{label,href}} />` renders 3 Buttons (primary, secondary, tertiary) — tertiary optional
    - `<FAQ items={faqs} />` renders `<section><h2>Preguntas frecuentes</h2><div class="faq-list"><details>...</details>...</div></section>` — native, zero JS, chevron rotates via CSS [open] selector
    - FAQ items respect reduced-motion (chevron transition wrapped in @media)
    - services.ts adds `ctaLabel` field per item: Acompañamiento → "Conocer el acompañamiento", Retiros → "Conocer los retiros"
    - faqs.ts exports 6 items matching D-13 LOCKED question list
  </behavior>
  <action>
    **FILE 1: `src/components/sections/ServiceTeaser.astro`**

    ```astro
    ---
    // src/components/sections/ServiceTeaser.astro
    // Phase 2 (CONT-01 home services split, D-22 Claude discretion on H3 element).
    // Title at --font-display-lg weight 500 (H3) — UI-SPEC hierarchy rule: weight differentiates H2 (300) vs H3 (500) at same size.
    // CTA is tertiary text-link (UI-SPEC §"ServiceTeaser CTA" labels LOCKED per card).

    import Button from '../ui/Button.astro';

    interface Props {
      title: string;
      teaser: string;
      ctaLabel: string;
      ctaHref: string;
    }
    const { title, teaser, ctaLabel, ctaHref } = Astro.props as Props;
    ---
    <article class="service-teaser">
      <h3>{title}</h3>
      <p class="teaser">{teaser}</p>
      <Button variant="tertiary" href={ctaHref} label={ctaLabel} />
    </article>

    <style>
      .service-teaser {
        padding: 32px 24px;
        background: var(--color-bg-elevated);
        border-radius: 12px;
        border: 1px solid var(--color-chrome-border);
        display: flex;
        flex-direction: column;
        gap: 16px;
        text-align: left;
      }
      h3 {
        font-family: var(--font-display);
        font-weight: 500;
        font-size: clamp(1.625rem, 4vw, 2rem); /* --font-display-lg */
        line-height: 1.25;
        letter-spacing: -0.005em;
        color: var(--color-text-primary);
        margin: 0;
      }
      .teaser {
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 1.0625rem; /* --font-body-md 17px */
        line-height: 1.6;
        color: var(--color-text-muted);
        margin: 0;
      }
    </style>
    ```

    **FILE 2: `src/components/sections/ServicesSplit.astro`**

    ```astro
    ---
    // src/components/sections/ServicesSplit.astro
    // Phase 2 (CONT-01 home step 3): 2-card services grid. 1col mobile, 2col tablet+.
    // Pattern adapted from src/pages/obras.astro lines 188–202 (3col grid — we cap at 2col).
    // BASE_URL helper applied to internal hrefs.

    import ServiceTeaser from './ServiceTeaser.astro';

    interface ServiceCard {
      slug: string;
      label: string;
      teaser: string;
      ctaLabel: string;
    }
    interface Props {
      services: ServiceCard[];
    }
    const { services } = Astro.props as Props;

    const baseUrl = import.meta.env.BASE_URL;
    const baseTrimmed = baseUrl.replace(/\/$/, '');
    const internal = (p: string) => {
      const path = p.replace(/^\//, '');
      return path === '' ? `${baseTrimmed}/` : `${baseTrimmed}/${path}`;
    };
    ---
    <section class="services-split">
      <div class="container">
        <ul class="services-grid" role="list">
          {services.map(s => (
            <li>
              <ServiceTeaser
                title={s.label}
                teaser={s.teaser}
                ctaLabel={s.ctaLabel}
                ctaHref={internal(`/${s.slug}`)}
              />
            </li>
          ))}
        </ul>
      </div>
    </section>

    <style>
      .services-split { padding-block: var(--space-section); }
      .services-grid {
        list-style: none;
        margin: 0;
        padding: 0;
        display: grid;
        grid-template-columns: 1fr;
        gap: 24px;
      }
      @media (min-width: 640px) {
        .services-grid { grid-template-columns: repeat(2, 1fr); }
      }
    </style>
    ```

    **FILE 3: `src/components/sections/CTABlock.astro`**

    ```astro
    ---
    // src/components/sections/CTABlock.astro
    // Phase 2 (CONT-06, D-05, D-06): 3-level jerarquía Primary (Calendly) > Secondary (WhatsApp) > Tertiary (form link).
    // Tertiary is OPTIONAL — pages may omit (e.g., /retiros never shows Calendly + WhatsApp, only application form).
    // Extracted from Phase 1 inline pattern in src/pages/contacto.astro lines 17–21 + 43–62, but tokens revised per UI-SPEC Button variants.

    import Button from '../ui/Button.astro';

    interface CTA {
      label: string;
      href: string;
      external?: boolean;
      ariaLabel?: string;
    }
    interface Props {
      primary: CTA;
      secondary?: CTA;
      tertiary?: CTA;
    }
    const { primary, secondary, tertiary } = Astro.props as Props;
    ---
    <section class="cta-block">
      <div class="container cta-row">
        <Button
          variant="primary"
          href={primary.href}
          label={primary.label}
          external={primary.external}
          ariaLabel={primary.ariaLabel}
        />
        {secondary && (
          <Button
            variant="secondary"
            href={secondary.href}
            label={secondary.label}
            external={secondary.external}
            ariaLabel={secondary.ariaLabel}
          />
        )}
        {tertiary && (
          <Button
            variant="tertiary"
            href={tertiary.href}
            label={tertiary.label}
            external={tertiary.external}
            ariaLabel={tertiary.ariaLabel}
          />
        )}
      </div>
    </section>

    <style>
      .cta-block { padding-block: var(--space-section); }
      .cta-row {
        display: flex;
        gap: 32px;
        flex-wrap: wrap;
        justify-content: center;
        align-items: center;
      }
      @media (max-width: 639px) {
        .cta-row {
          gap: 16px;
          flex-direction: column;
        }
        .cta-row > * { width: 100%; max-width: 20rem; }
      }
    </style>
    ```

    **FILE 4: `src/components/sections/FAQ.astro`**

    ```astro
    ---
    // src/components/sections/FAQ.astro
    // Phase 2 (CONT-08, D-13): 6 FAQs in native <details><summary>. Zero JS. Default closed.
    // Reduced-motion: chevron rotate transition wrapped (global override at global.css:60 also fires).
    // Summary at --font-display-lg weight 500 (H3-equivalent for visual hierarchy).

    interface FAQItem {
      question: string;
      answer: string;
    }
    interface Props {
      items: FAQItem[];
      heading?: string;
    }
    const { items, heading = 'Preguntas frecuentes' } = Astro.props as Props;
    ---
    <section class="faq">
      <div class="container">
        <h2>{heading}</h2>
        <div class="faq-list">
          {items.map(item => (
            <details class="faq-item">
              <summary>
                <span class="faq-q">{item.question}</span>
                <svg class="chevron" viewBox="0 0 16 16" width="16" height="16" aria-hidden="true">
                  <path d="M6 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round" />
                </svg>
              </summary>
              <div class="faq-answer">
                <p>{item.answer}</p>
              </div>
            </details>
          ))}
        </div>
      </div>
    </section>

    <style>
      .faq { padding-block: var(--space-section); }
      h2 {
        font-family: var(--font-display);
        font-weight: 300;
        font-size: clamp(1.625rem, 4vw, 2rem); /* --font-display-lg */
        line-height: 1.2;
        letter-spacing: -0.005em;
        color: var(--color-text-primary);
        margin: 0 0 48px;
        text-align: center;
      }
      .faq-list {
        max-width: 42rem;
        margin-inline: auto;
      }
      .faq-item {
        border-bottom: 1px solid var(--color-chrome-border);
      }
      .faq-item summary {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
        padding: 24px 0;
        cursor: pointer;
        list-style: none;
      }
      .faq-item summary::-webkit-details-marker { display: none; }
      .faq-q {
        font-family: var(--font-display);
        font-weight: 500;
        font-size: clamp(1.25rem, 3vw, 1.625rem); /* lower end of --font-display-lg */
        line-height: 1.25;
        color: var(--color-text-primary);
      }
      .chevron {
        flex-shrink: 0;
        color: var(--color-text-muted);
        transition: transform 200ms ease, color 200ms ease;
      }
      .faq-item[open] .chevron {
        transform: rotate(90deg);
        color: var(--color-orange-mystic);
      }
      .faq-answer {
        padding: 0 0 24px;
      }
      .faq-answer p {
        font-family: var(--font-body);
        font-weight: 400;
        font-size: 1.0625rem; /* --font-body-md 17px */
        line-height: 1.6;
        color: var(--color-text-muted);
        margin: 0;
        max-width: 38rem;
      }
      .faq-item summary:focus-visible {
        outline: 2px solid var(--color-orange-mystic);
        outline-offset: 4px;
        border-radius: 4px;
      }
      @media (prefers-reduced-motion: reduce) {
        .chevron { transition: none; }
      }
    </style>
    ```

    **FILE 5: `src/data/services.ts`** — REPLACE with extended shape

    ```ts
    // src/data/services.ts
    // Phase 2 (CONT-02, CONT-03, CONT-06): extended interface — adds ctaLabel per card (LOCKED in UI-SPEC),
    // optional fields description/audience/outcomes/confidentialityLine consumed by Plan 03 (acompañamiento) + Plan 04 (retiros).
    // Home services-split (Plan 02) only consumes slug + label + teaser + ctaLabel.

    export interface Service {
      slug: 'acompanamiento' | 'retiros';
      label: string;
      teaser: string;
      ctaLabel: string;
      description?: string;
      audience?: string;
      outcomes?: string[];
      confidentialityLine?: string;
    }

    export const services: Service[] = [
      {
        slug: 'acompanamiento',
        label: 'Acompañamiento',
        teaser: 'Encuentros uno-a-uno para C-Level, founders y nómadas digitales que están reordenando algo importante. Espacio de exploración no clínico.',
        ctaLabel: 'Conocer el acompañamiento',
      },
      {
        slug: 'retiros',
        label: 'Retiros',
        teaser: 'Tres fases — Preparación, Inmersión, Integración — para una pausa profunda. Aplicación previa obligatoria.',
        ctaLabel: 'Conocer los retiros',
      },
    ];
    ```

    **FILE 6: `src/data/faqs.ts`** — NEW per D-13

    ```ts
    // src/data/faqs.ts
    // Phase 2 (CONT-08, D-13): 6 FAQs consumed by FAQ.astro on home only.
    // Answers respect docs/copy-glossary.md PROHIBIDO list — no clinical attribution, no substance names.
    // Phase 3 may refine via abogado review; for Phase 2 these are the v1 written within glossary.

    export interface FAQItem {
      question: string;
      answer: string;
    }

    export const faqs: FAQItem[] = [
      {
        question: '¿Qué encuadre legal tiene este espacio?',
        answer: 'planetapsilo ofrece espacios de exploración personal y acompañamiento no clínico. No constituye terapia ni servicio de salud. Para necesidades de salud mental, consulta a un profesional habilitado.',
      },
      {
        question: '¿Qué tan confidencial es lo que se conversa aquí?',
        answer: 'Lo que se conversa aquí no sale de aquí. Trabajamos con discreción profesional y archivamos sólo lo mínimo necesario para responderte y coordinar próximos encuentros.',
      },
      {
        question: '¿Cómo sé si esto es para mí?',
        answer: 'Si estás en un momento de inflexión profesional o personal y buscas un espacio para reordenar desde otra altura, conversemos antes. Hay una conversación previa — para saber si lo que pides y lo que ofrecemos calzan.',
      },
      {
        question: '¿Qué pasa en un encuentro de acompañamiento?',
        answer: 'Una conversación uno-a-uno en un espacio sin interrupciones. Sin protocolo clínico, sin diagnóstico, sin recetario. La forma se ajusta a lo que cada persona necesita en cada momento.',
      },
      {
        question: '¿Cuánto tiempo dura un proceso típico?',
        answer: 'Depende. Algunas personas vienen por una conversación puntual; otras sostienen encuentros recurrentes durante varios meses. No hay número mínimo ni paquete obligatorio — se construye sobre la marcha.',
      },
      {
        question: '¿Cómo decido entre acompañamiento y un retiro?',
        answer: 'El acompañamiento es ongoing: encuentros que sostienen un proceso en tu vida cotidiana. El retiro es una pausa profunda concentrada en pocos días, con preparación e integración. Si nunca participaste en un espacio similar, conviene empezar por la conversación.',
      },
    ];
    ```
  </action>
  <verify>
    <automated>test -f src/components/sections/ServiceTeaser.astro && test -f src/components/sections/ServicesSplit.astro && test -f src/components/sections/CTABlock.astro && test -f src/components/sections/FAQ.astro && test -f src/data/faqs.ts && grep -q "Conocer el acompañamiento" src/data/services.ts && grep -q "Conocer los retiros" src/data/services.ts && grep -q "import Button from '../ui/Button.astro'" src/components/sections/ServiceTeaser.astro && grep -q "import Button from '../ui/Button.astro'" src/components/sections/CTABlock.astro && grep -q "<details" src/components/sections/FAQ.astro && grep -q "transform: rotate(90deg)" src/components/sections/FAQ.astro && grep -q "prefers-reduced-motion: reduce" src/components/sections/FAQ.astro && grep -c "question:" src/data/faqs.ts && [ "$(grep -c 'question:' src/data/faqs.ts)" = "6" ] && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist
    - `grep -c 'question:' src/data/faqs.ts` returns exactly 6 (six FAQ items)
    - `grep -c 'ctaLabel:' src/data/services.ts` returns 2 (one per service)
    - `grep -q "import Button" src/components/sections/CTABlock.astro` matches
    - FAQ.astro uses `<details>` AND has `[open] .chevron { transform: rotate(90deg) }` rule
    - ServiceTeaser h3 uses `font-weight: 500` AND `--font-display-lg` clamp (NOT 300)
    - ServicesSplit grid is 1col mobile and 2col at min-width 640px (matches UI-SPEC tablet breakpoint)
    - `npm run build` exits 0
    - `dist/_astro/*.css` contains `@media (prefers-reduced-motion: reduce)` (from FAQ chevron override)
  </acceptance_criteria>
  <done>4 components + 2 data files ready. Home page can compose them in Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create testimonials data + rewrite src/pages/index.astro composing all sections</name>
  <files>
    src/data/testimonials.ts,
    src/pages/index.astro
  </files>
  <read_first>
    - src/pages/index.astro (current Phase 1 — to REPLACE)
    - .planning/phases/02-mvp-content-three-channel-contact/02-UI-SPEC.md §"Per-page section composition" home steps 1–6 (lines 60–66), §"Per-page meta" home row (lines 474), §"Locked strings" rows H1, máxima anchor, BrandBio P1+P2, Testimonial disclaimer
    - .planning/phases/02-mvp-content-three-channel-contact/02-CONTEXT.md §D-03 (H1 LOCKED), §D-04 (máxima anchor), §D-12 (home testimonial topic), §D-14 (BrandBio paragraphs), §D-19 (waLink prefill text for home final CTABlock)
    - .planning/phases/02-mvp-content-three-channel-contact/02-PATTERNS.md §"Page-of-sections composition" (lines 133–157 — exact composition template)
    - All section components created in Tasks 1 + 2
    - docs/copy-glossary.md (verify all home copy passes glosario before commit — especially testimonial home variant Claude authors)
  </read_first>
  <behavior>
    - GET /planetapsilo/ returns 200
    - Renders 6 sections in LOCKED order: Hero → BrandBio → ServicesSplit → Testimonial (home) → FAQ → CTABlock
    - Title `planetapsilo` (no — pageTitle = "Inicio — planetapsilo" because BaseLayout composes `${title} — ${site.name}` when title is passed; UI-SPEC says title should be `planetapsilo` alone for home → pass no title prop OR title="" — BaseLayout falls back to `site.name`)
    - Per CONT-12 / UI-SPEC: home title is `planetapsilo` (no suffix). Solution: omit the `title` prop entirely so BaseLayout uses `site.name`.
    - Description: "Acompañamiento y retiros para liderazgos que están reordenando. Espacio de exploración personal para C-Level, founders y nómadas digitales."
    - Hero: variant home, kicker "planetapsilo", title LOCKED H1, maxim LOCKED anchor wording
    - BrandBio paragraphs from LOCKED P1 + P2 (P3 omitted in Phase 2 — Sofía will write later)
    - ServicesSplit pulls from services array (uses label + teaser + ctaLabel)
    - Testimonial: home variant from testimonials.ts + disclaimer LOCKED
    - FAQ pulls from faqs array, heading "Preguntas frecuentes"
    - Final CTABlock: primary Agenda una conversación → contact.calendlyUrl (external) / secondary Escribir por WhatsApp → waLink('Hola planetapsilo, me interesa explorar') (external) / tertiary "Prefiero escribir un formulario" → /contacto#form (internal)
  </behavior>
  <action>
    **FILE 1: `src/data/testimonials.ts`** — NEW

    ```ts
    // src/data/testimonials.ts
    // Phase 2 (CONT-07, D-11, D-12): 3 placeholder testimonials.
    // Phase 3 LEGAL-07 replaces these with real testimonios with archived written consent.
    // Format: initial + role + age + city. Disclaimer is the SAME LOCKED string per card.
    // Copy v1 by Claude within glosario PERMITIDO — Sofía refines wording before phase close.

    export interface Testimonial {
      context: 'home' | 'acompanamiento' | 'retiros';
      quote: string;
      attribution: string;
      disclaimer: string;
    }

    const DISCLAIMER = 'Testimonio ilustrativo de Phase 2 — voces reales consentidas en próxima versión. La experiencia individual no garantiza resultados.';

    export const testimonials: Testimonial[] = [
      {
        context: 'home',
        quote: 'Vine reordenando una venta y salí con algo más vivo que un plan.',
        attribution: 'M., founder fintech, 41 — Bogotá',
        disclaimer: DISCLAIMER,
      },
      {
        context: 'acompanamiento',
        quote: 'Llevábamos cuatro encuentros cuando entendí que el problema no era de equipo. Era una pregunta que yo no me estaba haciendo.',
        attribution: 'D., C-Level retail, 47 — Medellín',
        disclaimer: DISCLAIMER,
      },
      {
        context: 'retiros',
        quote: 'La integración me cambió más que la inmersión. La pausa profunda es donde algo se acomoda y después lo notas semanas después en cómo decides.',
        attribution: 'A., founder e-commerce, 38 — Bogotá',
        disclaimer: DISCLAIMER,
      },
    ];
    ```

    **FILE 2: `src/pages/index.astro`** — REPLACE entirely

    ```astro
    ---
    // src/pages/index.astro — Home (Phase 2)
    // Implements CONT-01 (hero + dual CTA), CONT-06 (CTABlock), CONT-07 (testimonial), CONT-08 (FAQ), CONT-12 (meta).
    // Section order LOCKED in UI-SPEC §"Per-page section composition `/` (home)".

    import BaseLayout from '../layouts/BaseLayout.astro';
    import Hero from '../components/sections/Hero.astro';
    import BrandBio from '../components/sections/BrandBio.astro';
    import ServicesSplit from '../components/sections/ServicesSplit.astro';
    import Testimonial from '../components/sections/Testimonial.astro';
    import FAQ from '../components/sections/FAQ.astro';
    import CTABlock from '../components/sections/CTABlock.astro';
    import { services } from '../data/services';
    import { testimonials } from '../data/testimonials';
    import { faqs } from '../data/faqs';
    import { contact, waLink } from '../data/contact';

    const homeTestimonial = testimonials.find(t => t.context === 'home');

    // BASE_URL helper for the tertiary form-link CTA target.
    const baseUrl = import.meta.env.BASE_URL;
    const baseTrimmed = baseUrl.replace(/\/$/, '');
    const contactForm = `${baseTrimmed}/contacto#form`;
    const retirosHref = `${baseTrimmed}/retiros`;

    // BrandBio LOCKED paragraphs (UI-SPEC §"Locked strings"). Both contain denial-clause "No es terapia" — copy linter exemption handled in Plan 06.
    const brandBioParagraphs = [
      'planetapsilo es un espacio de exploración personal para liderazgos que están reordenando. No es terapia ni servicio de salud — es acompañamiento y retiros pensados para mentes que ya construyeron mucho y ahora quieren ver desde otra altura.',
      'Cada propuesta recibe a pocas personas. Hay una conversación antes — para saber si lo que pides y lo que ofrecemos calzan. La pausa real no se improvisa.',
    ];
    ---
    <BaseLayout
      description="Acompañamiento y retiros para liderazgos que están reordenando. Espacio de exploración personal para C-Level, founders y nómadas digitales."
    >
      <Hero
        variant="home"
        kicker="planetapsilo"
        title="Acompañamiento y retiros para liderazgos que están reordenando."
        maxim="La claridad no se conquista. Se recuerda."
      >
        <div class="hero-cta-row">
          <a
            class="cta cta-primary"
            href={contact.calendlyUrl}
            target="_blank"
            rel="noopener noreferrer"
          >
            Agenda una conversación
          </a>
          <a
            class="cta cta-secondary"
            href={retirosHref}
          >
            Explora los retiros
          </a>
        </div>
      </Hero>

      <BrandBio paragraphs={brandBioParagraphs} />

      <ServicesSplit services={services} />

      {homeTestimonial && (
        <Testimonial
          quote={homeTestimonial.quote}
          attribution={homeTestimonial.attribution}
          disclaimer={homeTestimonial.disclaimer}
        />
      )}

      <FAQ items={faqs} />

      <CTABlock
        primary={{
          label: 'Agenda una conversación',
          href: contact.calendlyUrl,
          external: true,
        }}
        secondary={{
          label: 'Escribir por WhatsApp',
          href: waLink('Hola planetapsilo, me interesa explorar'),
          external: true,
          ariaLabel: 'Escribir a planetapsilo por WhatsApp',
        }}
        tertiary={{
          label: 'Prefiero escribir un formulario',
          href: contactForm,
        }}
      />
    </BaseLayout>

    <style>
      /* Hero dual CTA — uses .cta-primary + .cta-secondary inline to avoid 2nd Button import in the page itself.
         Visual rules match Button.astro primary/secondary exactly (UI-SPEC §"Button variants" — 12px 28px, pill 999px, 17px Inter 500). */
      .hero-cta-row {
        margin-top: 32px;
        display: flex;
        gap: 16px;
        justify-content: center;
        flex-wrap: wrap;
      }
      .cta {
        display: inline-block;
        font-family: var(--font-body);
        font-weight: 500;
        font-size: 1.0625rem;
        letter-spacing: 0.02em;
        line-height: 1.4;
        text-align: center;
        text-decoration: none;
        padding: 12px 28px;
        border-radius: 999px;
        box-sizing: border-box;
      }
      .cta:focus-visible {
        outline: 2px solid var(--color-orange-mystic);
        outline-offset: 2px;
      }
      .cta-primary {
        background: var(--color-orange-mystic);
        color: var(--color-bg-base);
        box-shadow: 0 8px 24px rgba(255, 122, 61, 0.35);
        transition: transform 200ms ease;
      }
      .cta-primary:hover { transform: scale(1.02); }
      .cta-primary:active { transform: scale(0.99); }
      .cta-secondary {
        background: transparent;
        color: var(--color-text-primary);
        border: 1px solid var(--color-text-primary);
        transition: background-color 200ms ease;
      }
      .cta-secondary:hover { background: rgba(244, 237, 227, 0.08); }

      @media (prefers-reduced-motion: reduce) {
        .cta-primary, .cta-secondary { transition: none; }
        .cta-primary:hover, .cta-primary:active { transform: none; }
      }
    </style>
    ```

    Note: Hero dual CTA reuses the same CSS rules as Button.astro inline-scoped rather than importing Button twice — keeps the Hero slot lightweight. The CSS values mirror Button.astro exactly (12px 28px, 999px, 17px, scale 1.02). Plan 07 audit will verify equivalence.
  </action>
  <verify>
    <automated>test -f src/data/testimonials.ts && grep -c "context:" src/data/testimonials.ts && [ "$(grep -c 'context:' src/data/testimonials.ts)" = "3" ] && grep -q "Acompañamiento y retiros para liderazgos que están reordenando." src/pages/index.astro && grep -q "La claridad no se conquista. Se recuerda." src/pages/index.astro && grep -q "import Hero" src/pages/index.astro && grep -q "import BrandBio" src/pages/index.astro && grep -q "import ServicesSplit" src/pages/index.astro && grep -q "import Testimonial" src/pages/index.astro && grep -q "import FAQ" src/pages/index.astro && grep -q "import CTABlock" src/pages/index.astro && grep -q "waLink('Hola planetapsilo, me interesa explorar')" src/pages/index.astro && grep -q "Prefiero escribir un formulario" src/pages/index.astro && npm run build && grep -q "Acompañamiento y retiros para liderazgos que están reordenando." dist/index.html && grep -q "La claridad no se conquista. Se recuerda." dist/index.html && grep -q "Agenda una conversación" dist/index.html && grep -q "Escribir por WhatsApp" dist/index.html && grep -q "Explora los retiros" dist/index.html && grep -q "Conocer el acompañamiento" dist/index.html && grep -q "Conocer los retiros" dist/index.html && grep -q "Preguntas frecuentes" dist/index.html && grep -c "<details" dist/index.html && [ "$(grep -c '<details' dist/index.html)" = "6" ]</automated>
  </verify>
  <acceptance_criteria>
    - `dist/index.html` contains EXACT H1 string: `Acompañamiento y retiros para liderazgos que están reordenando.`
    - `dist/index.html` contains EXACT máxima string: `La claridad no se conquista. Se recuerda.`
    - `dist/index.html` contains the LOCKED BrandBio P1 EXACTLY (with the "No es terapia ni servicio de salud" denial)
    - `dist/index.html` contains both ServiceTeaser CTAs: `Conocer el acompañamiento` and `Conocer los retiros`
    - `dist/index.html` contains exactly 6 `<details` opening tags (the 6 FAQs)
    - `dist/index.html` contains the home testimonial quote AND its attribution `M., founder fintech, 41 — Bogotá` AND the LOCKED disclaimer
    - `dist/index.html` contains the final CTABlock primary `Agenda una conversación` + secondary `Escribir por WhatsApp` + tertiary `Prefiero escribir un formulario`
    - `dist/index.html` contains link to `/planetapsilo/contacto#form` (tertiary form link)
    - `dist/index.html` contains `<title>planetapsilo</title>` (BaseLayout falls back to site.name when no title prop)
    - `dist/index.html` contains `<meta name="description" content="Acompañamiento y retiros para liderazgos que están reordenando. Espacio de exploración personal para C-Level, founders y nómadas digitales.">`
    - `dist/index.html` contains `<meta name="robots" content="noindex,nofollow">` (noindex preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Home renders all 6 LOCKED sections with LOCKED copy. CONT-01, CONT-06, CONT-07, CONT-08, CONT-09 (partial — confidentiality is on /acompanamiento /retiros, not home), CONT-12 (home row) addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → built static HTML | Hardcoded Spanish copy authored from CONTEXT.md decisions; cannot be tampered post-build (deploy artifact). |
| Browser → external links (Calendly + WhatsApp) | Tabnabbing risk — `rel="noopener noreferrer"` mandatory. |
| FAQ accordion | Native `<details>` — no JS interpretation of user input. Pure HTML. No injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | ServiceTeaser CTA labels | accept | Labels are LOCKED in UI-SPEC and hardcoded in data/services.ts; no user-supplied label can appear. |
| T-02-06 | Information Disclosure | Testimonial placeholders | mitigate | Disclaimer string LOCKED: "Testimonio ilustrativo de Phase 2 — voces reales consentidas en próxima versión." Renders on every Testimonial card; verified in acceptance criteria. |
| T-02-07 | Tampering | External Calendly + WhatsApp CTAs in Hero + CTABlock | mitigate | Hero uses inline `target="_blank" rel="noopener noreferrer"`; CTABlock delegates to Button (which sets these when `external=true`). Acceptance criteria asserts presence in dist/index.html. |
| T-02-08 | Repudiation | FAQ answers contain legal denial ("No constituye terapia ni servicio de salud" in FAQ #1) | mitigate | Phase 2 v1 FAQ copy authored within glosario PERMITIDO; flagged for abogado review in Phase 3 LEGAL-01. Linter exemption (Plan 06) handles denial-clause false-positives. |
</threat_model>

<verification>
After all 3 tasks:

1. `npm run build` exits 0.
2. `dist/index.html` is the FULL home page with all 6 sections present (verified via per-string greps in Task 3 acceptance criteria).
3. Spot-check the wave 3 dependents are unaffected: `dist/acompanamiento/index.html`, `dist/retiros/index.html`, `dist/contacto/index.html` still build (they remain Phase 1 stubs until Plans 03–05 land).
4. `grep -c "@media (prefers-reduced-motion: reduce)" src/components/sections/*.astro` returns >= 2 (FAQ chevron + any other component override that may have one).
5. `grep -rE "\\.\\./components/sections" src/pages/index.astro | wc -l` returns >= 6 (6 section imports).
6. `dist/index.html` final HTML size is < 80KB (no JS framework, no inline images).
</verification>

<success_criteria>
- 8 new components in `src/components/sections/`, 2 new data files in `src/data/`, services.ts extended, index.astro rewritten
- All LOCKED strings present in dist/index.html (verified by grep)
- Native `<details>` accordion with 6 items renders without any JS dependency
- Plan addresses CONT-01 (hero + dual CTA), CONT-06 (CTABlock component), CONT-07 (testimonial + disclaimer), CONT-08 (6 FAQs), CONT-12 (per-page meta home row)
- CONT-09 partially addressed: ConfidentialityLine component created and ready — actual placement on `/acompanamiento` and `/retiros` happens in Plans 03 + 04
</success_criteria>

<output>
After completion, create `.planning/phases/02-mvp-content-three-channel-contact/02-02-SUMMARY.md` covering:
- Section component library inventory (8 components, props shapes)
- Which LOCKED strings landed in dist/index.html
- Open items for Sofía: máxima wording refinement (D-04), testimonial wording refinement (D-11), BrandBio P3 (optional)
- Lighthouse mobile estimate (if locally run)
</output>
